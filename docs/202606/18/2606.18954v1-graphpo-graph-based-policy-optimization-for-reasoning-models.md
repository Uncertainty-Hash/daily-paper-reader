---
title: "GraphPO: Graph-based Policy Optimization for Reasoning Models"
title_zh: GraphPO：基于图的推理模型策略优化
authors: "Yuliang Zhan, Xinyu Tang, Jian Li, Dandan Zheng, Weilong Chai, Jingdong Chen, Jun Zhou, Ge Wu, Wenyue Tang, Hao Sun"
date: 2026-06-17
pdf: "https://arxiv.org/pdf/2606.18954v1"
tags: ["query:opd"]
score: 8.0
evidence: 基于图的策略优化用于RLVR推理模型
tldr: 针对RLVR中独立采样导致冗余探索和稀疏奖励的问题，提出GraphPO框架，将推理路径表示为有向无环图，合并语义等价节点并分配效率与正确性优势。理论证明可降低优势估计方差，在多个大语言模型上，相同预算下性能一致优于链式和树式基线，提升推理效率与效果。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.18954v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 731, \"height\": 354, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18954v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1446, \"height\": 412, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18954v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1431, \"height\": 867, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18954v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1452, \"height\": 421, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18954v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1442, \"height\": 386, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18954v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 513, \"height\": 377, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18954v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 513, \"height\": 341, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18954v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 519, \"height\": 366, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.18954v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1440, \"height\": 1051, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.18954v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1441, \"height\": 294, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.18954v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1440, \"height\": 357, \"label\": \"Table\"}]"
motivation: 独立采样响应导致冗余中间步骤和稀疏奖励，树方法分支无法共享信息且优势估计方差高。
method: 将rollouts构建为有向无环图，以语义状态为节点、推理步骤为边，合并等价路径，对入边赋予效率优势、出边赋予正确性优势。
result: 在多个大语言模型和推理/搜索基准上，相同token或响应预算下，GraphPO一致优于链式和树式基线。
conclusion: GraphPO通过图结构优化探索和优势估计，有效提升了推理效率和性能。
---

## 摘要
带可验证奖励的强化学习（RLVR）已成为增强大型推理模型能力的标准范式。RLVR通常独立采样响应，并仅利用最终答案来优化策略。该范式存在两个局限性：首先，独立生成的响应常包含相似的中间推理步骤，导致冗余探索和计算浪费；其次，稀疏的最终答案奖励难以识别有效步骤。基于树的方法通过共享前缀并比较同一前缀下的分支以提供细粒度信号，部分解决了该问题。然而，树的分支仍独立扩展。当不同分支达到相似推理状态时，它们无法共享信息，从而重复相似的探索。此外，基于树的方法忽略此类分散性，仅在独立分支内部进行局部比较，可能导致优势估计方差更高。为解决这一挑战，我们提出GraphPO（基于图的策略优化），一种新颖的RL框架，将轨迹表示为有向无环图，其中推理步骤作为边，从推理路径总结出的语义状态作为节点。GraphPO将语义等价的推理路径合并为等价类，使其能够共享后缀，并将预算从冗余扩展重新分配到多样化探索。此外，我们为入边分配效率优势，为出边分配正确性优势，从而在从结果推导过程监督的同时提高推理效率。理论表明，GraphPO降低了优势估计方差并提升了推理效率。在三个LLM上的推理和智能体搜索基准实验表明，GraphPO在使用相同词元预算或响应预算时，始终优于基于链和基于树的基线方法。

## Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) has become a standard paradigm for enhancing the capability of large reasoning models. RLVR typically samples responses independently and optimizes the policy using from final answers. This paradigm has two limitations. First, independently responses often contain similar intermediate reasoning steps, causing redundant exploration and wasted computation. Second, sparse final-answer rewards make it hard to identify useful steps. Tree-based methods partly address this problem by sharing prefixes and comparing branches from the same prefix to provide fine-grained signals. However, tree branches are still expanded independently. When different branches reach similar reasoning states, they cannot share information and repeat similar exploration. Moreover, tree-based methods ignore such dispersion and only perform local comparisons within separate branches, which can lead to higher variance in advantage estimation. To address this challenge, we propose GraphPO (Graph-based Policy Optimization), a novel RL framework that represents rollouts as a directed acyclic graph, with reasoning steps as edges and semantic states summarized from the reasoning paths as nodes. GraphPO merges semantically equivalent reasoning paths into equivalence classes, allowing them to share suffixes and reallocating budget away from redundant expansions to diverse exploration. Furthermore, we assign efficiency advantages to incoming edges and correctness advantages to outgoing edges, thereby improving inference efficiency while deriving process supervision from outcome. Theory shows that GraphPO reduces advantage-estimation variance and enhances reasoning efficiency. Experiments on three LLMs across reasoning and agentic search benchmarks show that GraphPO consistently outperforms chain- and tree-based baselines with the same token budgets or response budgets.

---

## 论文详细总结（自动生成）

# GraphPO：基于图的推理模型策略优化——详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

**核心问题：** 带可验证奖励的强化学习（RLVR）中，标准做法是独立采样多个响应，仅使用最终答案的二元奖励来优化策略。这导致两个严重缺陷：（1）独立响应常包含大量相似的中间推理步骤，造成冗余探索和计算浪费；（2）稀疏的最终奖励难以识别哪些中间步骤真正贡献了正确结果，即信用分配困难。尽管基于树的方法通过共享前缀并提供分支比较来部分缓解问题，但树结构仍然将分支视为独立路径——当不同分支达到语义等价的状态时，无法共享信息，依然会重复类似的探索；同时，树只进行局部比较，导致优势估计方差较高。

**整体含义：** 论文提出GraphPO，将推理采样过程建模为有向无环图（DAG），通过显式合并语义等价的中间状态，利用图结构同时改善推理效率、探索多样性和信用分配，从而在相同计算预算下获得更好的最终性能。

## 2. 论文提出的方法论

**核心思想**：将推理路径看作一个逐步构建的有向无环图（RL Graph），其中边代表生成的推理步骤，节点代表从初始提示到当前步骤所总结的语义状态。通过检测语义相似的状态并将它们合并为等价类，实现三个目标：
- 同一等价类内的不同路径可以共享后缀，获得更密集的奖励信号；
- 对到达同一等价类的不同路径，比较其长度，鼓励更高效的推理；
- 当一条路径到达已有等价类时，削减其后续扩展预算，将节省的资源重新分配给新的前沿状态，促进更广泛的探索。

**关键技术细节**：
1. **图构建（Graph Construction）**：从根节点（提示）开始逐层扩展。每生成一个推理步骤，使用策略模型本身将整条路径总结为语义状态，并通过嵌入模型（如SFR-Embedding-2-R）计算余弦相似度。当两个非祖先-后代节点的相似度超过阈值κ（实验中设为0.92）时，它们被合并到同一等价类中。合并后，直接后缀自动共享，并实施预算重新分配（到达等价类的路径的下一层预算减半）。
2. **图奖励（Graph Reward）**：对每个节点，通过其直接可到达的终端状态计算原始正确率。然后利用等价类内其他成员的后缀样本进行池化，得到更准确的节点分数。边奖励定义为节点分数之差乘以新颖性系数（1 - 语义相似度），从而既提供了过程监督，又对重复步骤进行折扣。
3. **双组图优势（Dual-Group Graph Advantage）**：
   - **正确性组**：比较从同一源节点出发的各个出边（直接边加上等价类共享的后继边），计算归一化的边奖励优势。
   - **效率组**：比较到达同一等价类的不同路径长度，结合该类的平均正确分数，将更高的优势分配给更短的路径。
4. **策略优化**：使用PPO风格目标，结合正确性优势和效率优势进行优化。

**公式与流程**：具体见论文第4.2–4.5节，核心公式包括节点分数池化公式（Eq.3）、边奖励公式（Eq.4）、正确性优势（Eq.5）、效率优势（Eq.6）、最终优势（Eq.7）以及PPO优化目标（Eq.8）。算法流程见附录Algorithm S.1。

## 3. 实验设计

**数据集/场景**：
- **数学推理**：AIME24、AIME25、MATH500。
- **其他推理**：GPQA（研究生级问答）、LiveCodeBench（代码）。
- **智能体深度搜索**：General AI Assistant、WebWalker、BrowseComp、XBench。

**基准方法**：
- 链式结果奖励RLVR：GRPO、DAPO。
- 树式推理方法：TreeRL、SPO、TREE-GRPO、PROS、TreePO。

**模型**：Qwen2.5-7B-Math、Qwen3-8B-Base、Deepseek-R1-Distill-Qwen-7B（推理任务），以及Qwen2.5-7B-Instruct（智能体任务）。

**实验设置**：控制相同的词元预算和响应预算；推理任务训练350步，智能体任务训练40步。详细超参数见附录C。

## 4. 资源与算力

论文明确说明实验使用 **16 × H20 GPU**（附录C）。推理任务中，每个训练步骤对应一个batch（batch size 512，minibatch size 32）。智能体任务batch size 128，minibatch size 64。所有实验在Verl框架（推理）和Search-R1框架（智能体）上实现，使用vLLM进行高效推理。未报告具体训练时长，但提到在相同轨迹数量下GraphPO显著减少了训练时间（图5b）。

## 5. 实验数量与充分性

**实验组数**：
- **主实验**：4个LLM × 5个数据集的性能对比（表1），共20个主实验结果。
- **智能体任务**：1个模型 × 4个数据集（表2），4个结果。
- **消融实验**：
  - 合并阈值κ的影响（图6）。
  - 池化系数w的影响（图S.2）。
  - 效率优势的作用（图S.1）。
  - 不同分支策略（表S.1）。
- **分析实验**：
  - 训练动态（准确率、熵、响应长度，图4）。
  - 轨迹生成效率（图5c）。
  - 与PRM方法对比（图5a）。
  - 相同轨迹预算下的性能与训练时间（图5b）。

**充分性**：实验覆盖多种模型规模、多个任务类型、多种预算条件。消融实验系统分析了关键超参数的影响。对比基线涵盖了当前主流的链式和树式RLVR方法。公平性方面，严格控制了词元预算或响应预算。整体实验设计较为全面且客观。

## 6. 论文的主要结论与发现

1. **GraphPO在所有模型和所有推理/智能体基准上一致优于链式和树式基线**，在相同预算下平均性能提升3–6个百分点（见表1、表2）。
2. **图结构有效减少了冗余探索**：训练过程中GraphPO的响应长度持续最短，而树方法先减后增（图4c）；GraphPO的熵衰减最慢，表明保持了更好的探索多样性（图4b）。
3. **效率优势推动了更高效推理**：消融实验显示，去掉效率优势后响应长度会再次增长（图S.1）。
4. **无需标注过程数据即可获得过程监督**：与PRM方法对比（图5a），GraphPO在无需额外标注的情况下性能更优，理论证明其梯度方向与新颖性门控的PRM一致。
5. **图结构在智能体搜索任务中同样有效**（表2），因为它天然适配多步决策。
6. **中等合并阈值（κ≈0.92）和中等池化系数（w≈0.7）获得最佳性能**，过高或过低都会损害效果（图6、图S.2）。
7. **理论分析**：共享后缀降低了优势估计方差（定理1），预算重分配提升了探索效率（定理2），效率优势倾向于选择更短的等价正确路径（定理4）。

## 7. 优点

- **创新性**：将图结构引入RLVR，通过合并语义等价状态，同时解决了冗余探索、信用分配稀疏和效率问题，突破了树结构的局限。
- **理论扎实**：对优势估计方差、探索效率、梯度对齐、路径长度偏好均给出了严格的理论证明（附录F）。
- **实验全面**：覆盖多种模型、多种任务、多种预算，并有丰富消融和分析，结论可靠。
- **实用性强**：在相同计算预算下显著提升性能，且不依赖额外标注数据，训练效率也有提升（图5b）。
- **鲁棒性分析**：通过理论证明了错误检测（假阳性和假阴性）的影响可控，不会导致性能崩溃（附录G）。

## 8. 不足与局限

- **实验覆盖范围有限**：论文主要聚焦数学推理和智能体搜索，尚未系统评估在多模态推理、代码生成（带执行反馈）、长周期交互环境等更广泛场景下的效果（附录H）。
- **依赖语义检测**：图构建依赖于嵌入模型的相似度计算和总结质量。虽然论文理论分析了鲁棒性，但实际效果仍受限于检测精度和阈值选择，可能需要针对不同领域调整参数。
- **计算开销**：尽管总体预算控制，但图构建需要额外的嵌入推理和相似度比较，在超大规模部署中可能成为瓶颈。论文未详细分析计算开销的分解。
- **仅考虑二元奖励**：RLVR设定只使用正确/错误信号，若任务具有更丰富的奖励（如部分正确或进度奖励），GraphPO能否直接利用或需修改未讨论。
- **分支策略启发式**：每层分支数量是手工设定的，论文虽给出了不同策略的比较（表S.1），但缺乏自适应分支机制。
- **基线选择**：虽然对比了多种树方法，但未与最新的MCTS类方法（如AlphaMath）进行对比；也未与基于蒙特卡洛值估计的过程监督方法（如VinePPO）进行对比。

（完）
