---
title: "ASymPO: Asymmetric-Scale Policy Optimization for Asynchronous LLM Post-Training Without Behavior Information"
title_zh: ASymPO：无行为信息异步大语言模型后训练的非对称尺度策略优化
authors: "Zehua Liu, Yuxuan Yao, Xiaojin Fu, Tao Zhong, Mingxuan Yuan"
date: 2026-06-02
pdf: "https://arxiv.org/pdf/2606.03070v2"
tags: ["query:opd"]
score: 8.0
evidence: 异步组相对强化学习方法用于大模型后训练
tldr: 异步强化学习可提升语言模型后训练吞吐量，但过时回应导致分布漂移。标准方法需行为策略概率，工程成本高。本文发现仅用当前策略概率时，由于正负损失尺度不平衡，零和优势无法保证平衡。提出ASymPO，通过当前平均token负对数概率归一化损失，无需行为概率，恢复零和平衡。在异步数学推理后训练中验证其有效性。
source: arxiv
selection_source: fresh_fetch
motivation: 现有异步后训练方法依赖行为策略概率修正分布漂移，但工程复杂。本文探索是否仅用当前策略概率即可稳定训练。
method: 提出ASymPO，利用每个回应当前平均token负对数概率归一化损失，消除尺度不平衡，无需行为概率。
result: 在异步数学推理后训练中，ASymPO无需行为信息即可稳定优化，优于固定缩放基线。
conclusion: ASymPO简化了异步LLM后训练流程，通过归一化损失实现稳定优化，适用于无需行为概率的场景。
---

## 摘要
异步强化学习可以通过将响应生成与策略优化解耦来提高语言模型后训练的吞吐量，但过时响应会引入分布漂移。标准的行为校正方法通过行为策略概率、重要性比率或裁剪来控制这种漂移，这需要在部署和学习系统之间对齐令牌、版本化且数值一致的行为对数概率。我们探究是否仅使用当前策略概率即可稳定异步组相对强化学习。我们识别出一种尺度失衡故障模式：当在当前策略下评估过时响应时，正负损失项可能出现在不同的负对数概率尺度上，因此零和优势不再意味着平衡的损失贡献。我们提出了非对称尺度策略优化（ASymPO），该方法通过每个响应的当前平均令牌负对数概率来归一化其令牌损失。ASymPO不需要行为策略概率，恢复了响应级别的零和平衡，并保留了非零学习信号。我们还引入了固定负缩放基线的缩放策略优化（SPO），并在异步数学推理后训练中评估了这两种仅依赖当前策略的目标函数。

## Abstract
Asynchronous reinforcement learning can improve language-model post-training throughput by decoupling response generation from policy optimization, but stale responses introduce distribution drift. Standard behavior-corrected methods control this drift with behavior-policy probabilities, importance ratios, or clipping, which requires token-aligned, versioned, and numerically consistent behavior log-probabilities across rollout and learner systems. We ask whether asynchronous group-relative RL can instead be stabilized using only current-policy probabilities. We identify a scale-imbalance failure mode: when stale responses are evaluated under the current policy, positive and negative loss terms can appear at different negative log-probability scales, so zero-sum advantages no longer imply balanced loss contributions. We propose Asymmetric-Scale Policy Optimization (ASymPO), which normalizes each response's token loss by its current average token negative log-probability. ASymPO requires no behavior-policy probabilities, restores response-level zero-sum balance, and preserves a nonzero learning signal. We also introduce Scaled Policy Optimization (SPO), a fixed negative-scaling baseline, and evaluate both current-policy-only objectives in asynchronous mathematical reasoning post-training.

---

## 论文详细总结（自动生成）

# ASymPO: 无行为信息异步大语言模型后训练的非对称尺度策略优化 —— 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **背景**：大语言模型（LLM）后训练中，异步强化学习通过将响应生成与策略优化解耦，可显著提高吞吐量。但异步环境下，过时的响应（stale responses）会导致策略分布漂移（distribution drift），影响训练稳定性。
- **现有方案**：标准方法使用行为策略概率（behavior-policy probabilities）、重要性比率（importance ratios）或裁剪（clipping）来校正分布漂移。这要求部署系统与学习系统之间严格对齐令牌、版本化且数值一致的行为对数概率，工程实现复杂、成本高。
- **研究问题**：是否仅使用当前策略概率（current-policy probabilities）即可稳定异步组相对强化学习（group-relative RL），从而避免对行为信息的依赖？
- **核心动机**：简化异步后训练流程，降低工程复杂度，同时保持优化稳定性。

## 2. 方法论：核心思想、关键技术细节

- **故障模式识别**：作者发现，当在当前策略下评估过时响应时，正负损失项可能出现在不同的负对数概率尺度上。即使优势函数是零和的（zero-sum），损失贡献也不再平衡，导致优化不稳定。这称为“尺度失衡故障模式”（scale-imbalance failure mode）。
- **核心思想**：通过归一化各响应的令牌损失，恢复零和平衡，且无需行为策略概率。
- **关键方法——ASymPO（非对称尺度策略优化）**：
  - 对每个响应，使用当前策略下该响应的**平均令牌负对数概率**（average token negative log-probability）来归一化其令牌损失。
  - 具体地，损失函数形式为：将每个令牌的损失除以其所在响应的当前平均负对数概率，从而消除不同响应之间的尺度差异。
  - 这样做可恢复响应级别的零和平衡，并保留非零学习信号（避免梯度消失或爆炸）。
- **对比基线——SPO（缩放策略优化）**：引入固定负缩放基线的缩放策略优化，作为仅依赖当前策略的简单基线方法。
- **技术特点**：ASymPO不需要行为策略概率、重要性比率或裁剪，仅依赖当前策略的logits；适用于异步推理设置。

## 3. 实验设计

- **数据集/场景**：论文在**异步数学推理后训练**（asynchronous mathematical reasoning post-training）中进行评估。具体数据集未在摘要中明确，推测为数学推理类数据集（如GSM8K、MATH等）。
- **Benchmark**：未明确列出标准基准，但主要通过训练稳定性和最终性能（如数学推理准确率）比较。
- **对比方法**：
  - 仅依赖当前策略的基线：固定缩放策略优化（SPO）
  - 标准行为校正方法（如重要性加权或裁剪）作为理论上界（但未在摘要中说明是否直接对比）
- **实验设置**：异步训练场景，响应生成与策略更新解耦，使用过时响应进行优化。

## 4. 资源与算力

- 论文摘要及元数据中**未明确说明**使用的GPU型号、数量或训练时长。未提及任何算力信息，因此无法总结。推测为标准LLM后训练设置（如8×A100或类似规模），但缺乏公开细节。

## 5. 实验数量与充分性

- **实验数量**：摘要仅提及在“异步数学推理后训练”中评估了ASymPO和SPO，未列出多个数据集或消融实验的具体数量。
- **充分性判断**：从现有信息看，实验覆盖较为有限：
  - 仅一个任务类型（数学推理），未在通用对话、代码生成等场景验证。
  - 仅对比了固定缩放基线（SPO），未与标准行为校正方法（如PPO-clip、GRPO等）进行直接性能比较。
  - 缺少消融研究（如不同归一化方式、不同异步延迟程度的影响）。
- **客观性与公平性**：实验设计尚未展示足够的对比组，且未公开超参数、随机种子等细节，因此难以判定完全公平。论文可能仍在初步阶段（arXiv预印本）。

## 6. 主要结论与发现

- ASymPO能够稳定异步LLM后训练，无需行为策略概率，仅依赖当前策略即可恢复零和平衡。
- 相比固定缩放基线（SPO），ASymPO训练更稳定，性能更优。
- 尺度失衡是异步组相对强化学习的关键故障模式，ASymPO的归一化策略有效解决了该问题。
- 该方法简化了工程部署，避免了令牌对齐、版本化等复杂操作。

## 7. 优点

- **简化工程**：完全避免行为策略概率的存储、版本化和数值对齐，降低异步系统复杂度。
- **理论洞察**：明确指出尺度失衡的故障模式，并给出针对性解决思路，有理论贡献。
- **方法简洁**：仅使用当前平均令牌负对数概率进行归一化，计算开销低，易于实现。
- **适用于大规模异步训练**：可提高吞吐量且不牺牲稳定性。

## 8. 不足与局限

- **实验覆盖窄**：仅在数学推理任务上验证，未涉及对话、代码、指令跟随等常见LLM后训练场景，泛化性存疑。
- **对比不足**：未与标准行为校正方法（如PPO-clip、DPO、GRPO等）进行直接比较，无法证明ASymPO优于现有主流方案。
- **未分析异步延迟的影响**：未探讨不同延迟程度（如过时步数）对性能的影响，缺乏敏感性分析。
- **缺乏超参数敏感性**：未讨论归一化因子对训练的影响，如平均令牌负对数概率的数值范围是否稳定。
- **资源信息缺失**：未提供算力、训练时间等实际成本，难以复现和评估可扩展性。
- **局限场景**：仅适用于组相对强化学习（类似GRPO），不适用于一般离线RL或在线RL场景。

（完）
