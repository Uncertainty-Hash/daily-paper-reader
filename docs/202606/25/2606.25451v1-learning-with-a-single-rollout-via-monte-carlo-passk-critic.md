---
title: "Learning with a Single Rollout via Monte Carlo Pass@k Critic"
title_zh: "通过蒙特卡洛Pass@k评论家以单次展开进行学习"
authors: "Fengdi Che, Yang Liu, Lei Yu, Meng Cao, Tong Che, Rupam Mahmood, Dale Schuurmans"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.25451v1"
tags: ["query:opd"]
score: 8.0
evidence: 通过单rollout PPO和token级信用改进GRPO
tldr: "语言模型强化学习中，组内多次采样导致的轨迹异质性使信用分配困难且昂贵。本文提出单次rollout的PPO（SR-PPO），通过训练基于Pass@k成功概率的token级评论家来估计优势，避免了重复采样。随着k增大，Pass@k收敛为可达性指标，可在状态图上线性时间计算，提供了一种无需对比采样的直接信用分配信号。在HMMT26和AIME24数学推理基准上，SR-PPO表现出稳定的学习动态，并一致提升了Pass@128成功率。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25451v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1439, \"height\": 873, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25451v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1455, \"height\": 483, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25451v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1428, \"height\": 350, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25451v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1366, \"height\": 757, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25451v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1376, \"height\": 901, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25451v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1467, \"height\": 222, \"label\": \"Table\"}]"
motivation: 解决语言模型RL中因反复采样导致轨迹发散，使得token级优势估计成本高且信用分配困难的问题。
method: "提出SR-PPO，使用单次rollout的蒙特卡洛结果训练一个校准的token级评论家，预测Pass@k成功概率以估计优势。"
result: "在HMMT26和AIME24上，SR-PPO实现了稳定学习，并在Pass@128成功率上取得一致提升。"
conclusion: "Pass@k作为信用信号优于Pass@1，且极限情况可高效计算，为直接信用分配提供了有效替代方案。"
---

## 摘要
在语言模型的强化学习（RL）中，估计令牌级优势仍然具有挑战性，因为扩展情节经验收集的成本很高。对于基线优势估计方法，困难进一步加剧，因为重复采样导致轨迹发散为本质上不同的推理前缀。在这种情况下，像GRPO这样的RL算法显得有限：结果奖励过于稀疏，无法归因于中间步骤等具体动作，并且不同采样轨迹之间的比较因异质性而变得困难。为了减轻重复采样的计算成本和信用分配的难度，我们研究了单次展开近端策略优化（SR-PPO），它在语言模型的RL中实现了令牌级信用分配。与通过在候选组内归一化情节回报来估计优势不同，我们使用每个提示一次展开的蒙特卡洛结果训练一个经校准的令牌级信用评论家。具体来说，我们使用该评论家来预测提示前缀处的Pass@k成功概率，该概率源自一次Pass@1尝试。这一选择产生了比Pass@1更具选择性的学习信号：它降低了易于解决的前缀的权重，同时优先考虑成功概率仍然较低的困难前缀。我们证明，随着$k$增加，Pass@k收敛到一个可达性指标，反映了一个前缀是否可以导致至少一次成功继续。在显式状态图中，极限（$k \rightarrow \infty$）可以在$O(|V|+|E|)$时间内计算，为直接信用分配提供了一个有希望的替代方案，而无需采样对比轨迹。作为初步验证，SR-PPO展示了稳定的学习动态，并在HMMT26和AIME24等数学推理基准上持续提高了Pass@128成功率。

## Abstract
Estimating token-level advantages in reinforcement learning (RL) for language models remains challenging because scaling up episodic experience collection is expensive. The difficulty intensifies for baseline advantage estimation methods, where repeated sampling causes trajectories to diverge into substantially different reasoning prefixes. In this context, RL algorithms such as GRPO prove limited: an outcome reward is too sparse to be attributed to specific actions like intermediate steps, and comparisons across sampled traces are non-trivial because they are heterogeneous. To mitigate both the computational cost of repeated sampling and the difficulty of credit assignment, we study single-rollout proximal policy optimization (SR-PPO) featuring token-level credit assignment in RL for language models. Instead of estimating advantages by normalizing episodic returns within the candidate group, we train a calibrated token-level credit critic using Monte Carlo outcomes from one rollout per prompt. Specifically, we use the critic to predict the Pass@k success probability at the prompt prefix, which is derived from a Pass@1 attempt. This choice yields a more selective learning signal than Pass@1: it discounts easily solved prefixes while prioritizing hard ones whose success probability remains marginal. We show that as $k$ increases, Pass@k converges to a reachability indicator, reflecting whether a prefix can lead to at least one successful continuation. In an explicit state graph, the limit ($k \rightarrow \infty$) can be computed in $O(|V|+|E|)$ time, offering a promising surrogate for direct credit assignment without the need to sample contrastive traces. As an initial validation, SR-PPO exhibits stable learning dynamics, along with consistent gains in Pass@128 success rates on mathematical reasoning benchmarks such as HMMT26 and AIME24.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：在语言模型的强化学习（RL）中，基于结果奖励的监督过于稀疏，难以对推理过程中的中间步骤（token级）进行有效信用分配。传统方法如GRPO依赖组内多次采样来归一化优势，但多次采样成本高且轨迹异质性大（不同rollout可能调用不同工具、采用不同分解策略），导致对比噪声大。为此，作者提出在**单次rollout**（每个提示仅采样一条完整轨迹）的约束下进行token级信用分配，以降低采样成本并克服轨迹异质性问题。
- **整体含义**：探索一种不依赖多条对比轨迹的密集信用分配方法，通过训练一个**前缀级信用评论家**预测最终成功概率，实现单rollout下的有效训练。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：
  - 使用**蒙特卡洛Pass@k**作为评论家的预测目标：对每个前缀状态预测在$k$次独立采集中至少有一个成功完成的概率。
  - 通过比较相邻前缀的Pass@k值变化来构造**token级优势**，并结合终端校准项对齐最终结果。
  - 用诱导的Pass@1值（反变换）来训练评论家，但实际优势用Pass@k计算，以获得更为选择性、稳定的学习信号。

- **关键技术细节**：
  1. **Pass@k价值族**：$q_{\pi,k}(s_t) = 1 - (1 - q_{\pi}(s_t))^k$，其中$q_{\pi}(s_t)$是Pass@1成功概率。用评论家网络直接输出$\hat{q}_{\pi,k}(s_t)$，再通过逆变换$ \hat{q}_{\pi}(s_t) = 1 - (1 - \hat{q}_{\pi,k}(s_t))^{1/k}$获得诱导Pass@1用于损失函数。
  2. **损失函数**：$L_{\text{credit}} = \frac{1}{T+1}\sum_{t=0}^T \ell_t(\phi) + \lambda_{\text{prompt}}\ell_0(\phi)$，其中$\ell_t(\phi) = BCE(\hat{q}_{\pi,t}, Y) + \lambda_{\text{Brier}}(\hat{q}_{\pi,t} - Y)^2$，同时包括交叉熵和Brier分数以校准概率。
  3. **Token级优势**：$A_{\pi,k}(s_t) = \Delta \hat{q}_{\pi,k}(s_t) + \lambda(Y - \hat{q}_{\pi,k}(s_T))$，其中$\Delta \hat{q}_{\pi,k}(s_t) = \hat{q}_{\pi,k}(s_t) - \hat{q}_{\pi,k}(s_{t-1})$，终端校准项$\lambda(Y - \hat{q}_{\pi,k}(s_T))$使密集信号与实际结果对齐（$\lambda=1$）。
  4. **策略优化**：使用on-policy单步更新目标$J(\theta) = \frac{1}{M} \sum_{i=1}^B \sum_{t=1}^{T_i} r_{i,t}(\theta) A_{i,t}^{(k)}$。在每batch新采样后，$\theta = \theta_{\text{old}}$，因此clip分支无效，实质上是带有密集token信用的单步策略梯度。

- **与可达性的联系**：当$k \to \infty$时，Pass@k收敛到二元可达性指标$r_{\pi}(s) = \mathbb{1}\{q_{\pi}(s) > 0\}$。在显式状态图上，可达性可通过反向图遍历$O(|V|+|E|)$时间精确计算。实际使用$k=4$作为折中。

### 3. 实验设计：数据集、Benchmark、对比方法
- **基础模型**：Qwen3-1.7B（thinking模式），最大响应长度9182 tokens。
- **训练数据**：DeepScaleR数学推理数据集。
- **评估Benchmark**：AIME 2024、AIME 2025、HMMT 2026（均为竞赛级数学推理）。
- **对比方法**：
  - **GRPO**（组相对策略优化，每个提示8个rollout）。
  - **GAE-PPO**（使用广义优势估计的PPO，单rollout基准）。
  - **Pass@1 SR-PPO**（使用k=1单rollout信用评论家）。
  - **Pass@4 SR-PPO**（主要方法）。
- **评估指标**：Pass@8验证成功率、Pass@128保持集上的Pass@k曲线、格式分数等。

### 4. 资源与算力
- 论文**未明确说明**所使用的GPU型号、数量及训练时长。仅提及使用Qwen3-1.7B模型在DeepScaleR数据集上训练，训练步数最多180步（见于图3等）。未提供具体的算力消耗细节。

### 5. 实验数量与充分性
- **实验数量**：
  - 主验证：Pass@8学习曲线（图1b）对比GRPO、GAE-PPO、SR-PPO。
  - 消融实验：Pass@1 vs Pass@4 SR-PPO（图1c），不同梯度累积步数。
  - 保持集评估：AIME24/25、HMMT26上Pass@k曲线（图3）。
  - Token优势稀疏度统计（表1）。
  - 定性可视化（附录C）。
- **充分性与客观性**：
  - 实验覆盖了单rollout场景下的主要对比，并提供了GRPO（多rollout）作为强基线。
  - 但**仅使用了一个模型大小（1.7B）、一个数据集、一次随机种子**，未跨多模型、多种子、多域验证。
  - 超参数选择有限（如k只试了1和4），未系统探索k的影响。
  - 总体实验规模较小，结论的泛化性有待加强。

### 6. 论文的主要结论与发现
- **单rollout方法可行**：Pass@4 SR-PPO在Pass@8验证上达到与GRPO（使用8倍rollout）相当的性能，证明单rollout训练也能有效提升多采样推理。
- **Pass@k信号更稳定**：Pass@4的优势具有更高稀疏性（~50% token优势接近零），减少了不必要的更新，防止Pass@1训练的不稳定性。
- **Pass@1 vs Pass@4的取舍**：Pass@4 SR-PPO在Pass@1上并非最优（其信用目标偏向多样本成功），但多采样表现更好。
- **GAE-PPO在单rollout下失效**：标准TD-bootstrapping无法有效学习。
- **可达性视角**理论上有意义，但实际中使用k=4作为软代理。

### 7. 优点
- **方法创新**：提出基于Pass@k的单rollout信用分配，将稀疏结果奖励转化为密集token级信号，同时引入可达性解释。
- **计算高效**：训练时每个提示只需一次rollout，大幅降低采样成本，适用于长链条和代理任务。
- **理论扎实**：给出Pass@k收敛到可达性的严格证明（Lemma A.1、A.2），并展示在已知状态图上可达性可线性时间计算。
- **实验设计直接**：通过归一化rollout预算（图1b）公平比较采样效率；消融实验清晰展示Pass@4的稳定性和稀疏性优势。

### 8. 不足与局限
- **实验覆盖有限**：仅使用Qwen3-1.7B单一模型，未在更大模型或不同架构（如Mamba、MoE）上验证；数据集仅限DeepScaleR数学推理，未推广到代码、代理任务或多轮对话。
- **单次随机种子**：结果可能不具备统计显著性（未报告多次随机种子下的均值和方差）。
- **超参数未系统探索**：k值仅试了1和4，未探索其他k值（如2、8、16）的影响；评论家训练系数、优势系数也未充分调参。
- **缺乏与最新方法的对比**：未与TreePO、VIMPO、PRM等方法比较（但论文定位为单rollout与GRPO的比较，可理解为初始验证）。
- **理论落地有差距**：可达性计算需要完整状态图，这在实践中通常不可得；实际训练仍依赖学习到的评论家，存在近似误差。
- **泛化风险**：由于训练数据与评估基准同属数学推理，方法可能具有领域特定性，在更异构的环境中效果未知。

（完）
