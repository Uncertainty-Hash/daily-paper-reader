---
title: Understanding Diversity Collapse in RLVR via the Lens of Overtraining
title_zh: 理解RLVR中的多样性崩溃：过训练的视角
authors: "Suqin Yuan, Jinkun Chen, Jiyang Zheng, Muyang Li, Lei Feng, Dadong Wang, Tao Xiang, Tongliang Liu, Bo An"
date: 2026-06-13
pdf: "https://arxiv.org/pdf/2606.15455v1"
tags: ["query:opd"]
score: 9.0
evidence: 研究RLVR中的多样性崩塌现象，用过度训练视角形式化该问题
tldr: "强化学习与可验证奖励（RLVR）常导致多样性崩溃：Pass@1提升但高k Pass@k下降，反映推理边界变窄。本文从过训练视角形式化该现象：问题对指标的贡献饱和后，更新不再扩展边界，反而集中概率质量于采样偏好的轨迹。提出贝叶斯边界门控（BBG），通过估计每个问题对边界的边际贡献来重定向优化。在多个推理基准上，BBG显著改善广泛k的Pass@k平均值，并表明RLVR能扩展基础模型推理能力。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15455v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1441, \"height\": 412, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15455v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1428, \"height\": 419, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15455v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1440, \"height\": 381, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15455v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 551, \"height\": 425, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15455v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1424, \"height\": 1101, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15455v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1434, \"height\": 840, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15455v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1422, \"height\": 437, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.15455v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1438, \"height\": 1065, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15455v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1452, \"height\": 403, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15455v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 946, \"height\": 568, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15455v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 995, \"height\": 431, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15455v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 396, \"height\": 225, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15455v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1368, \"height\": 106, \"label\": \"Table\"}]"
motivation: 揭示RLVR多样性崩溃的机制，从过训练视角解释其根源，并设计方法缓解对推理边界的负面影响。
method: 贝叶斯边界门控（BBG）估计每个问题对推理边界的边际贡献，仅对未饱和问题施加优化更新以避免过训练。
result: "限制更新于零成功问题在困难基准上使Pass@256超过基础模型；BBG在多个推理基准上改善广泛k的Pass@k平均值。"
conclusion: 过训练视角统一理解多样性崩溃，BBG有效缓解该问题并释放RLVR扩展推理能力的潜力。
---

## 摘要
基于可验证奖励的强化学习（RLVR）已成为增强大型语言模型推理能力的关键方法。然而，RLVR常常遭受“多样性崩溃”的问题：Pass@$1$提升的同时，高$k$的Pass@$k$下降，这被视为模型推理边界变窄。我们通过“过训练”的视角形式化了这一多样性崩溃：一旦一个问题对参考指标的贡献实际上达到饱和，进一步的更新不再扩展模型能解决的问题，但仍会将概率质量集中在在线策略采样偏好的轨迹上。在标准设置下，每个问题仅有少量采样轨迹，即使单次成功观测也会使问题在高$k$的Pass@$k$上处于接近饱和的状态，因此标准RLVR中的大多数更新从边界角度来看都是过训练。这一视角也解读了RLVR是否能够扩展模型的推理能力超越基础模型：由于RLVR在结构上对高$k$的Pass@$k$存在偏差，其整体下降本身并不意味着没有产生新的推理收益。在干预上，将更新限制在零成功观测的问题上，在困难基准上将Pass@$256$提升至基础模型之上；在观察上，在标准RLVR训练过程中，一部分初始无法解决的问题变得可解。基于这些发现，我们提出了“贝叶斯边界门控”（BBG），通过估计每个问题对推理边界的边际贡献来将优化从过训练中转移开。在多个推理基准上，BBG在广泛的$k$值范围内提升了平均Pass@$k$。

## Abstract
Reinforcement learning with verifiable rewards (RLVR) has become a key approach for enhancing the reasoning abilities of large language models. However, RLVR often suffers from \emph{diversity collapse}: Pass@$1$ improves while high-$k$ Pass@$k$ degrades, which is viewed as a narrowing of the model's reasoning boundary. We formalize this diversity collapse through the lens of \emph{overtraining}: once a problem's contribution to the reference metric has effectively saturated, further updates no longer expand what the model can solve but still concentrate probability mass on the trajectories favored by on-policy sampling. Under a standard setup with few rollouts per problem, even a single observed success places a problem in a nearly saturated regime for high-$k$ Pass@$k$, so most updates in standard RLVR are overtraining from the boundary perspective. This perspective also suggests a reading of whether RLVR can expand the model's reasoning abilities beyond the base model: since RLVR is structurally biased against high-$k$ Pass@$k$, its aggregate decline does not by itself mean that no new reasoning gains occurred. Interventionally, restricting updates to problems with zero observed success lifts Pass@$256$ above the base model on difficult benchmarks; observationally, a non-trivial fraction of initially unsolvable problems become solvable during standard RLVR training. Building on these findings, we propose \emph{Bayesian Boundary Gating} (BBG), which redirects optimization away from overtraining by estimating each problem's marginal contribution to the reasoning boundary. Across multiple reasoning benchmarks, BBG improves average Pass@$k$ across a wide range of $k$.

---

## 论文详细总结（自动生成）

# 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：基于可验证奖励的强化学习（RLVR）已成为提升大语言模型推理能力的关键范式，但训练中普遍出现“多样性崩溃”现象——Pass@1 提升的同时，高 k 的 Pass@k 频繁下降。这被解读为模型推理边界收缩。先前工作将多样性归因于在线策略的自强化，但未能区分“哪些更新仍在扩展解决能力，哪些仅是在已能解决的轨迹间重分配概率质量”。
- **整体含义**：论文通过 **“过训练”（overtraining）** 视角重新理解多样性崩溃。作者证明：在标准 RLVR 设置下，大部分更新实际上是对已饱和问题的过度优化，对扩大推理边界无益，反而加剧了边界收缩。论文进一步指出，高 k Pass@k 的整体下降并不证明 RLVR 无法获得新的推理能力，而是被过训练掩盖。最后提出 **贝叶斯边界门控（BBG）** 方法，通过估计每个问题对推理边界的边际贡献来重定向优化，显著缓解多样性崩溃。

# 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：
  - 将 RLVR 的训练过程分解为对每个问题的更新，定义“边界效用”衡量单位提升问题成功率对高 k Pass@k 的边际贡献。
  - 由于训练中每个问题仅观察到少量轨迹（如 n=8），即使只观测到一次成功，其成功率估计已使高 k Pass@k 接近饱和（例如 Pass@256 几乎为 1），此时进一步更新无法扩大边界，只会导致过训练。
  - 因此，应将优化资源集中在零成功轨迹的问题上（B₀/n），这些更新才可能真正扩展边界。

- **关键技术细节**：
  - **贝叶斯边界效用估计**：假设问题真实成功率 p 服从 Beta 先验，根据观测到的 m 次成功和 n 次轨迹得到后验分布 Beta(m+1, n-m+1)。然后计算对 J_k(p) = 1 - (1-p)^k 的期望边际贡献：
    ```
    u(m; n, k) = E_{p ~ Beta(m+1, n-m+1)}[k(1-p)^{k-1}] = k * ∏_{j=1}^{k-1} (n - m + j) / (n + 1 + j)
    ```
  - **贝叶斯边界门控（BBG）**：对每个问题，根据其观测成功 m 计算 u(m; n, k)。设置阈值 τ（论文用 0.01），若 u(m; n, k) < τ/(n+1) 则完全门控该问题（不参与反向传播）。否则保留，并将其贡献权重归一化。BBG 叠加在带符号的验证损失上，仅调节问题级系数，不改变轨迹级损失。
  - **参数**：n=8, k=16（几何中点）, τ=0.01。结果 B₀/₈、B₁/₈、B₂/₈、B₃/₈ 被保留，B₄/₈ 及以上被门控。

- **算法流程**：
  1. 对每个训练问题采样 n 个轨迹，计算正确计数 m。
  2. 根据 m 查表得到 u(m; n, k)。
  3. 若 u 低于阈值则丢弃该问题，否则保留并计算归一化权重。
  4. 仅使用保留的问题执行策略梯度更新（采用 W-RF 优势权重）。

# 3. 实验设计：使用了哪些数据集/场景，它的 benchmark 是什么，对比了哪些方法

- **数据集**：
  - **训练集**：MATH 训练集（约 7500 个竞赛级数学题）。
  - **评测基准**：
    - MATH-500（分布内）
    - OlympiadBench（675 道国际奥赛题，分布外）
    - MinervaMath（272 道大学级定量推理题，分布外）
    - AIME 2025（30 道美国数学邀请赛题，最困难）
- **对比方法**：
  - **REINFORCE**：标准策略梯度，使用二元奖励 r∈{-1,+1} 和指数移动平均基线。
  - **GRPO**（Shao et al., 2024）：组内优势归一化。
  - **W-RF**（Zhu et al., 2025）：强调负强化，降低正奖励权重至 0.3。
  - **BBG（本文方法）**：在 W-RF 基础上增加问题级门控。
  - 此外还测试了 **早停**（epoch 5/10）和 **桶掩码**（mask B₈/₈ 等）作为消融。

# 4. 资源与算力

- **训练资源**：单节点配备 4 张 NVIDIA H100 96GB GPU，使用 FSDP 和梯度检查点。
- **推理**：单张 H100 GPU，使用 vLLM（bfloat16）。
- **训练时长**：每轮训练共 140 步（约 20 个 epoch），每 epoch 约 7 步（batch size 1024）。未明确总训练时间，但提到使用 4 GPU 集群。
- 论文未给出精确的 GPU 小时数，但规模为 7B 模型，属于中等算力消耗。

# 5. 实验数量与充分性

- **实验组数**：
  - 主线实验：4 个评测基准 × 4 种方法（REINFORCE、GRPO、W-RF、BBG）→ 16 个训练轨迹。
  - 消融实验（表 2）：早停（3 个 epoch 点）+ 桶掩码（3 种模式）+ BBG→ 约 7 种变体。
  - 边界追踪实验（图 3、4）：追踪问题跨 epoch 的可解状态变化，含采样噪声分析。
  - 其他分析：Pass@1 过训练（图 2a），Pass@k 过训练（图 2b），以及随 epoch 的 Pass@k 曲线（附录 D）。
- **充分性与公平性**：
  - 对比方法均使用相同超参数（表 3），且基于同一训练框架 verl，公平性较好。
  - 评估使用无偏 Pass@k 估计（Chen et al., 2021），温度 0.6，top-p 0.95，每问题 256 个样本。
  - 消融实验覆盖了简单的早停和桶掩码，验证过训练分析的正确性。
  - 不足之处：所有实验仅在单一训练集（MATH 训练集）和单一模型（Qwen2.5-Math-7B）上进行，未验证更大模型或更复杂训练混合下的泛化性。不过论文在局限性中明确指出了这一点。

# 6. 论文的主要结论与发现

- **过训练是多样性崩溃的根源**：标准 RLVR 中，最多 91.8% 的更新涉及至少一次成功的轨迹，而这些更新对高 k Pass@k 的边际贡献几乎为零，反而会锐化策略、导致边界收缩。
- **RLVR 确实能扩展推理边界**：通过观察初始不可解问题在训练中变为可解，以及将更新限制在零成功桶（B₀/₈）可提升 Pass@256，证明 RLVR 能获得新推理能力，但该增益被过训练掩盖。
- **BBG 方法有效缓解多样性崩溃**：在全部 4 个基准上，BBG 均取得最高的平均 Pass@k，且是唯一在所有基准上 Pass@256 不低于基础模型的方法。尤其在 MinervaMath 和 OlympiadBench 上显著优于其他方法。
- **简单干预也有效**：早停、桶掩码等简单策略同样能恢复部分高 k 性能，证实过训练分析的正确性。

# 7. 优点：方法或实验设计上的亮点

- **理论分析清晰**：从 Pass@k 对成功率的指数饱和性出发，严格给出过训练的数学定义（式 7），并联系到训练中的实际观测（图 1a），使得“大多数更新是无用优化”这一直觉得到形式化支持。
- **贝叶斯框架处理不确定性**：采用 Beta-Binomial 后验估计真实成功率，从而计算期望边际效用，避免直接使用有偏的观测值，更鲁棒。
- **简单有效的干预**：BBG 仅修改问题级权重，不改变轨迹级损失，易于实现且与现有方法（如 W-RF）可叠加。
- **实验设计巧妙**：通过追踪初始不可解问题的状态（图 3）和限制更新到 B₀/₈ 的干预实验（图 4），直接论证 RLVR 能扩展边界，反驳了先前工作“RLVR 不能扩展推理能力”的结论。
- **消融研究充分**：通过早停和桶掩码，展示了从过训练视角衍生的多种缓解策略，且效果与理论一致。

# 8. 不足与局限

- **实验泛化性有限**：仅在 7B 模型和 MATH 训练集上验证，未在更大模型（如 70B）或更多样化的训练混合（如代码、逻辑推理）上测试。论文明确承认此局限。
- **Pass@1 与平均 Pass@k 的权衡**：BBG 虽然提升平均 Pass@k，但 Pass@1 相比 W-RF 略有下降（如在 MinervaMath 上 37.1% vs 39.4%）。论文指出若单样本精度是首要指标，该权衡可能不理想。
- **零成功桶的噪声**：由于 n≪k，零成功桶可能包含实际已达边界的问题（即成功概率小但 Pass@256 已很高），导致门控策略不够精确。论文在附录 B 中讨论了此问题并建议增加 n 以改善。
- **未考虑 token 级信用分配等更细粒度原因**：论文聚焦于问题级过训练，承认多样性崩溃可能还有其他原因（如 token 级信用分配困难、奖励稀疏等）。
- **AIME 2025 上的改进主要来自负强化**：BBG 在 AIME 上表现最好，但 W-RF 同样在大多数 k 上优于基础模型，暗示负强化本身对困难题贡献大；BBG 的额外提升需进一步分解。

（完）
