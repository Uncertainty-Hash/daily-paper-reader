---
title: "Breaking the Tokenizer Barrier: On-Policy Distillation across Model Families"
title_zh: 打破分词器障碍：跨模型家族的在线策略蒸馏
authors: "Yifan Niu, Han Xiao, Dongyi Liu, Zelong Wang, Dihong Gong, Yasheng Wang, Jia Li"
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.09456v1"
tags: ["query:opd"]
score: 10.0
evidence: 跨模型族的同策略蒸馏在大语言模型中的应用
tldr: 在线策略蒸馏（OPD）是大型语言模型后训练中从专家模型向学生模型传递知识的关键技术，但现有方法要求师生模型共享同一分词器，限制了跨模型族应用。本研究提出了一种跨分词器的OPD方法，通过精确的token映射算法，使标准OPD能够在不共享分词器的情况下传递高保真token级信号。实验表明，该方法在多种基准上比基于SFT的基线方法计算效率显著更高。这一成果解锁了更广泛的师生对组合，为适应和增强LLM间交互开辟了新途径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.09456v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 645, \"height\": 373, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09456v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 853, \"height\": 521, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09456v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1393, \"height\": 593, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.09456v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1123, \"height\": 565, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.09456v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1438, \"height\": 250, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.09456v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1458, \"height\": 454, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.09456v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1437, \"height\": 928, \"label\": \"Table\"}]"
motivation: 现有OPD方法要求师生模型共享同一分词器，限制了跨模型族的应用；而SFT无法捕捉教师概率分布中的丰富知识。
method: 提出一种跨分词器的OPD方法，利用精确的token映射算法，使标准OPD能在不同tokenizer间传递token级信号。
result: 在多种基准上，跨分词器OPD相比基线方法（如SFT）计算效率显著更高。
conclusion: 跨分词器OPD解锁了更广泛的师生对组合，为LLM交互与适应提供了新途径。
---

## 摘要
在线策略蒸馏（OPD）已成为大语言模型（LLM）后训练中从领域专家向学生模型转移知识的核心技术。然而，现有的OPD蒸馏方法要求教师与学生模型共享同一分词器，这限制了OPD在模型系列中的适用性。当前主流实践通常采用在教师生成响应上进行监督微调（SFT）以实现跨分词器蒸馏，但这种方式未能捕捉教师概率分布中蕴含的丰富知识。在本工作中，我们使标准在线策略蒸馏方法能够跨模型家族运行，通过精确的令牌映射算法确保高保真的令牌级信号能够跨越不同分词器传播。大量实验表明，跨分词器OPD在各种基准测试中比基线方法显著计算效率更高。我们的结果为OPD解锁了更广泛的教师-学生对组合，为适应和增强LLM之间的交互开辟了新途径。

## Abstract
On-Policy Distillation (OPD) has become a core technique in the post-training of Large Language Models (LLMs) for transferring knowledge from domain experts to student models. However, existing OPD distillation methods require teacher and student models to share the same tokenizer, restricting the applicability of OPD within the model series. Current mainstream practice typically employs Supervised Fine-Tuning (SFT) on teacher-generated responses for cross-tokenizer distillation, which fails to capture the rich knowledge embedded in the teacher's probability distribution. In this work, we enable the standard on-policy distillation method to operate across model families, ensuring that high-fidelity token-level signals can propagate across different tokenizers with a precise token-mapping algorithm. Extensive experiments show that cross-tokenizer OPD is significantly more compute-efficient than baselines on various benchmarks. Our results unlock a broader range of teacher-student pairs for OPD, opening up new avenues for adapting and enhancing interactions between LLMs.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有在线策略蒸馏（On-Policy Distillation, OPD）要求教师模型与学生模型共享同一个分词器（tokenizer），这严重限制了OPD在不同模型家族之间的应用。当前跨分词器的常见做法是使用监督微调（SFT）在教师生成的静态序列上训练学生，但这种方法无法利用教师概率分布中蕴含的丰富密度信息，导致知识传递不充分。
- **研究动机**：尽管不同分词器的词汇表存在差异，但都基于相同的Unicode/字节级底层表示，并且字符级片段存在结构对应关系。论文试图利用这一共性，突破分词器壁垒，实现跨家族的高保真令牌级信号传递。
- **整体含义**：通过提出自适应分块对齐算法和基于语义先验的信用分配机制，首次实现了无需共享分词器的高保真在线策略蒸馏，显著扩大了师生对的适用范围，为LLM后训练提供了更灵活、高效的监督方式。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：将学生自回归生成的响应重新用教师分词器编码，通过双指针贪婪搜索找到两端分词序列之间的最小同步块（minimal synchronized chunks），然后将教师在该块上的总对数概率按学生原始对数概率的比例（语义先验）分配到学生各个令牌上，从而构建令牌级优势信号，再基于标准的ON-policy RL目标来更新学生模型。
- **关键技术细节**：
  - **自适应分块对齐（DPCA）**：维护学生和教师序列的两个指针，基于严格单调的解码属性，采用“追赶”策略：比较当前累积解码文本长度，短的一侧向前扩展一个令牌，直到两端解码文本完全相等，形成一对同步块。算法保证**完备性**（不遗漏任何同步点）和**最小性**（每个块不能再拆分为更小的同步子序列）。
  - **块级信用分配**：在一个同步块内，假设教师和学生生成该段文本的联合概率应当相等，从而块内总对数概率相等。为了保持学生的内部语义结构，求解一个受约束的最小二乘优化（目标为投影后的对数概率与学生先验对数概率的对数差距最小，约束为总和对数概率等于教师块的总和），得到闭式解：
    \[
    \log q_i = \frac{L_T^{(c)}}{L_S^{(c)}} \log p_i, \quad \hat{A}_{i,t} = \left( \frac{L_T^{(c)}}{L_S^{(c)}} - 1 \right) \log p_i
    \]
    其中 \(L_T^{(c)}\), \(L_S^{(c)}\) 分别为教师和学生在该块的总对数似然，\(p_i\) 为学生先验对数概率。该分配使得学生原本越“惊讶”的令牌获得越大的优势，且在1:1对齐时退化为标准OPD。
  - **训练目标**：采用与标准OPD相同的RL重要性采样损失（式(10)），将上述计算得到的每令牌优势 \(\hat{A}_{i,t}\) 代入，并引入重要性采样比和裁剪。

### 3. 实验设计：使用了哪些数据集 / 场景、benchmark、对比方法

- **数据集**：
  - **SFT初始化数据**：从OpenThoughts（涵盖数学、代码、科学问答、通用推理等26个来源，由QwQ-32B注释）选取400K样本。
  - **OPD训练数据**：分别从OpenThoughts和DeepMath各抽取20K prompt（共40K样本），用于在线训练。
- **场景设置**：
  1. **Qwen3-8B (教师) → Llama3.1-8B-SFT (学生)**：跨模型家族（不同分词器）。
  2. **DeepSeek-R1 (教师) → DeepSeek-R1-Distill-Qwen-7B (学生)**：强大教师蒸馏至已有初步蒸馏的学生。
- **Benchmark**：AIME24/25/26（数学竞赛）、MATH-500（数学）、GPQA-Diamond（科学问答）、LiveCodeBench（代码生成）。评价指标：AIME和MATH使用答案提取和精确匹配，GPQA使用多选题准确率，LiveCodeBench使用测试用例准确率。
- **对比方法**：
  - **SFT**（传统方法）
  - **ALM**[22]：基于表面形式块的跨分词器静态似然匹配。
  - **CDM**[20]：基于上下文依赖的动态映射策略进行更细粒度的似然匹配。
  - 额外比较了**同分词器OPD**（Qwen3→Qwen3）以分离投影步骤的影响。

### 4. 资源与算力（如果提到）

- 论文训练超参数详见附录表4，但**未明确给出GPU型号、数量及训练时长**。
- 提供了计算FLOPs估算（表2），以Qwen3-8B→Llama3.1-8B-SFT为例：为达到AIME24 44.4%准确率，SFT需要额外482K数据，教师和学生的总FLOPs分别为 \(6.0\times10^{20}\) 和 \(5.7\times10^{20}\)，合计约 \(1.17\times10^{21}\) FLOPs；而OPD只需额外20K数据，教师预填充和学生训练总FLOPs约 \(4.8\times10^{19}\) FLOPs，仅为SFT的约4%。
- 使用的框架：SFT阶段用LLaMA-Factory，OPD阶段用VeRL，教师服务单独部署。
- 结论：OPD在计算效率上具有显著优势，但具体硬件资源未披露。

### 5. 实验数量与充分性

- **主要实验**：表1展示了两个设置下SFT、ALM、CDM、OPD的六项基准成绩，共8行对比（每个设置4行），结果清晰。
- **附加分析**：
  - 同tokenizer vs 跨tokenizer OPD增益对比（表3）：两个设置均值差异仅0.1。
  - 计算成本分析（图2、表2）：展示随数据量增长的性能曲线及FLOPs估算。
  - 定性案例分析（图3）：展示分块及信用分配的具体例子。
- **充分性评价**：实验覆盖了数学、科学问答、代码三个领域，包含两个跨家族场景及一个同家族对比，并进行了计算效率的量化对比。消融方面通过ALM/CDM对比体现了OPD在在线信号上的优势。实验设计较为充分、客观。但**缺乏在更大规模模型（如70B）上的验证**。

### 6. 论文的主要结论与发现

1. **跨分词器OPD显著优于SFT及现有跨分词器方法**：在两个设置下，OPD在全部6个基准上均取得最佳平均分，且增益远高于ALM和CDM。
2. **计算效率极大提升**：以AIME24达到44.4%为例，OPD仅需SFT约4%的数据量，总FLOPs约为SFT的4%。
3. **分词器不对齐不削弱OPD增益**：同tokenizer OPD增益5.5，跨tokenizer OPD增益5.6，表明提出的对齐与投影方法完整保留了密集监督信号。
4. **方法普适性强**：在从通用教师到已蒸馏学生、以及从强教师到不同家族学生两种场景下均有效。

### 7. 优点

- **创新性强**：首次系统解决跨分词器OPD难题，提出可证明的DPCA算法和闭式信用分配公式。
- **理论保障**：证明了分块算法的完备性与最小性，保证了高分辨率信号传递。
- **实验设计完善**：涵盖多个基准、两个重要场景、与SOTA跨分词器方法对比，并进行了计算成本分析。
- **实际价值高**：解锁了更灵活的师生配对，可节省大量计算资源，对工业界后训练有直接指导意义。
- **代码开源**：提供GitHub仓库，增强可复现性。

### 8. 不足与局限

- **模型规模局限**：实验仅局限于8B参数以下的模型（教师和学生），未在更大规模（如70B）上验证，结论的泛化性有待确认。
- **缺少详细硬件与时长**：未报告GPU型号、数量、训练时间，读者难以精确复现或评估实际资源需求。
- **潜在偏差风险**：OPD训练数据仅来自OpenThoughts和DeepMath，可能偏向推理任务（数学和代码），对其他任务（如对话、写作）的效果未评估。
- **依赖文本对齐假设**：方法假设同一文本在不同分词器下字符级完全对应，对于多语言或包含特殊标记的文本是否依然鲁棒未讨论。
- **消融不完整**：虽对比了ALM和CDM，但未消融DPCA算法本身（例如使用固定粗粒度分块与最小分块的对比），也未消融信用分配中语义先验的必要性。

（完）
