---
title: "OPD+: Rethinking the Advantage Design for On-Policy Distillation"
title_zh: "OPD+: 重新思考在线策略蒸馏中的优势设计"
authors: "Hanyang Zhao, Haoxian Chen, Han Lin, Genta Indra Winata, David Yao, Wenpin Tang"
date: 2026-05-31
pdf: "https://arxiv.org/pdf/2606.01039v1"
tags: ["query:opd"]
score: 9.0
evidence: 重新思考在线策略蒸馏的优势设计
tldr: 在线策略蒸馏(OPD)中，现有方法为稳定性采用停止梯度设计，导致优势估计有偏。本文基于f-散度构建通用优化框架，从数学上证明了停止梯度操作会造成奖励和梯度估计偏差。提出修正版本OPD+，支持多种f-散度，在数学推理和工具使用基准上超越基线KL方法。贡献在于纠正了OPD中的理论缺陷并提升了性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.01039v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1467, \"height\": 496, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01039v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 721, \"height\": 563, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.01039v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 798, \"height\": 190, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01039v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1483, \"height\": 510, \"label\": \"Table\"}]"
motivation: 现有OPD的停止梯度设计虽稳定，却使优势估计有偏，需要重新审视其有效性。
method: 提出基于f-散度的通用优化框架，数学上证明停止梯度导致偏差，并修正为OPD+。
result: OPD+在数学推理和工具使用基准上优于基线KL方法，支持多种f-散度。
conclusion: 证明了停止梯度设计有偏，OPD+纠正了理论缺陷并提升性能。
---

## 摘要
在线策略蒸馏（OPD）是一种广泛使用的技术，用于将能力从强教师语言模型迁移到基础学生模型，并且可以表述为一种使用学生生成轨迹的强化学习风格目标。然而，尽管散度奖励依赖于学生模型似然，现有工作通常为了稳定性而采用停止梯度设计，这使得得到的优势估计存疑。在这项工作中，我们提供了一个基于学生和教师之间f-散度的通用优化框架，并从数学上重新审视这种设计空间是否有效。我们证明，对于一般散度函数，通用的停止梯度操作会导致奖励目标及其对应梯度的有偏估计。我们提出了OPD+，这是OPD的修正版本，在基线KL方法上展示了改进的性能，并且支持各种f-散度的选择。我们在数学推理和工具使用基准上验证了我们的发现。

## Abstract
On-policy distillation (OPD) is a widely used technique to transfer capabilities from capable teacher language models to the base student models, and can be formulated in a reinforcement learning style objective using student generated rollouts. Yet, despite the divergence reward being dependent on student model likelihood, existing works usually adopt a stop gradient design primarily for stability, which makes the resulting advantage estimation questionable. In this work, we provide a generic optimization framework based on f-divergence between the student and teacher, and mathematically revisit whether such design space is valid. We prove that general stop-gradient operation would lead to biased estimates of the reward objective and corresponding gradient for general divergence functions. We propose OPD+, the corrected version of OPD that demonstrates improved performance over the baseline KL approach and also supports the choice of various f-divergence. We validate our findings on mathematical reasoning and tool-use benchmarks.

---

## 论文详细总结（自动生成）

# 论文总结：OPD+: Rethinking the Advantage Design for On-Policy Distillation

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：在线策略蒸馏（OPD）中，现有方法普遍采用停止梯度（stop gradient）来处理学生与教师间的散度奖励（如KL散度），以确保数值稳定性。但该设计使得优势估计在数学上有偏差——学生模型的参数同时影响奖励函数，停止梯度操作忽略了这一依赖关系。
- **研究动机**：重新审视OPD中奖励/优势的设计空间，是否真的可以停止梯度？对于不同散度（如Forward KL、JSD），停止梯度是否导致训练崩溃？修正后的设计能否提升性能？
- **整体含义**：作者证明了停止梯度会导致一般f-散度的奖励和梯度估计有偏，并提出了修正版本OPD+，支持多种f-散度（Forward KL、Reverse KL、JSD），在数学推理和工具使用任务上显著优于基线KL方法，挑战了当前工业界普遍偏好Reverse KL的实践。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：基于f-散度构建通用优化框架，推导真实的梯度（包含奖励对θ的导数项），纠正停止梯度引起的偏差。
- **关键技术细节**：
  - 定义token级密度比 \( u_{\theta,n} = q(y_n | \cdot)/p_\theta(y_n | \cdot) \)。
  - 目标函数 \( J_f(\theta) = \mathbb{E}_{x,y\sim p_\theta} \sum_{n} f(u_{\theta,n}) \)。
  - 停止梯度下的梯度估计为 \( \nabla_\theta J_f^{sg} = \mathbb{E}[\sum_n \nabla_\theta \log p_\theta(y_n) \cdot (-f(u_{\theta,n}))] \)。
  - 正确梯度应包含直接奖励梯度项：\( \nabla_\theta r_f^\theta = u_{\theta,n} f'(u_{\theta,n}) \nabla_\theta \log p_\theta(y_n) \)。
  - 因此正确优势为 \( w_f(u) = -f(u) + u f'(u) \)（见表1）。
  - 特例：Reverse KL（f(u)=-log u）下 \( w(u) = -\log u + 1 \)，常数+1项在期望中消失，故停止梯度近似正确；但Forward KL和JSD则完全不同，停止梯度会导致训练崩溃。
  - OPD+仅需一行代码修改：将奖励从 \(-f(u)\) 替换为 \( w_f(u) \)。

## 3. 实验设计：数据集、基准、对比方法
- **数据集与任务**：
  - 竞赛数学：使用DeepMath数据集蒸馏，评估在AIME 24、AIME 25、HMMT 25上的avg@32准确率。
  - 工具使用：使用ToolUse任务（68个问题测试集），同样报告avg@32。
  - 自蒸馏（OPSD）：模型自身作为教师（利用特权信息），在AIME 24上评估Qwen3-1.7B。
- **模型**：Qwen3-1.7B和8B（Base模型经过SFT初始化）。
- **对比方法**：对于每种散度（Forward KL、JSD、Reverse KL），比较标准OPD（使用停止梯度优势 \(-f(u)\)）与OPD+（使用修正优势 \(w(u)\)）。还包括SFT初始基线。
- **结果**：表2展示了各方法的最佳检查点性能，以及训练动态图（Figure 1、Figure 2）。

## 4. 资源与算力
- 文中**未明确说明**使用的GPU型号、具体数量及训练时长。
- 仅提到超参数：数学任务100个在线策略步骤（batch size 64组），工具任务80个步骤（256个rollouts/步），学习率1e-6（OPSD中）等。
- 因此算力细节缺失，无法评估实验成本。

## 5. 实验数量与充分性
- **实验数量**：
  - 主实验（表2）：3种散度 × 2种优势设计 × 3个数学基准 + 工具任务 + 自蒸馏，共约16组结果。
  - 训练动态图（Figure 1）：展示了各任务在100步内的性能曲线，验证收敛和稳定性。
  - OPSD实验（Figure 2）：额外一组消融。
  - 未进行大规模模型规模分析或跨模型族验证。
- **充分性与公平性**：
  - 充分：覆盖了多种f-散度、两种训练范式（OPD和OPSD）、两个应用领域，结果清晰显示OPD+消除崩溃并提升性能。
  - 公平：统一超参数，使用官方推荐基线设置，但作者承认可能通过超参搜索进一步提升。
  - 局限：只使用Qwen3家族，未在更大规模或不同架构模型上验证；未比较logit蒸馏与单token蒸馏；未对变体进行大量消融。

## 6. 论文的主要结论与发现
- **数学正确性**：证明停止梯度操作对于一般f-散度会引入梯度偏置，OPD+给出正确的梯度形式。
- **性能提升**：
  - OPD+使Forward KL和JSD从训练崩溃（准确率0%）被拯救至与Reverse KL竞争甚至超越的水平。
  - JSD+在AIME 25（50.42% vs 48.54%）和ToolUse（60.29% vs 59.97%）上超越标准Reverse KL。
  - Reverse KL自身也因OPD+得到微小提升（AIME 24: 64.69% vs 64.27%），并加速收敛（Figure 1）。
- **挑战传统认知**：反向KL并非唯一稳定选项；其他f-散度在正确优化下同样有效甚至更优。

## 7. 优点：方法或实验设计上的亮点
- **理论贡献**：首次系统分析OPD中停止梯度的数学偏差，提供统一f-散度框架及修正公式。
- **简洁实用**：修正仅需一行代码，易于集成到现有OPD流水线。
- **实验验证充分**：覆盖多个散度、两个任务领域（数学推理和工具使用）、两种蒸馏范式（OPD和OPSD），结果一致支持结论。
- **可视化训练动态**：通过性能曲线展示了OPD+加速收敛和避免崩溃的能力，增强说服力。

## 8. 不足与局限
- **模型规模有限**：仅使用Qwen3-1.7B和8B，未扩展到更大模型（如70B），结论的泛化性需进一步验证。
- **未做模型族外验证**：仅测试Qwen3，其他架构（如Llama、Mistral）结果未知。
- **未比较logit蒸馏形式**：文中只采用单token似然蒸馏，未考虑logit级蒸馏不同散度的效果。
- **算力资源未报告**：缺少GPU型号、数量、训练时长等关键信息，影响可复现性评估。
- **超参调优不深入**：承认可能通过更细粒度超参搜索获得更好结果，当前对比可能不是最优。
- **消融实验不足**：未系统分析修正项中不同部分的影响，或与其它基线（如PPO-clip）的比较。

（完）
