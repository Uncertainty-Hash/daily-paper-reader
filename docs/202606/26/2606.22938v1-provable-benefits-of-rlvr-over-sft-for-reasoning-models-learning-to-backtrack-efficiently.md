---
title: "Provable Benefits of RLVR over SFT for Reasoning Models: Learning to Backtrack Efficiently"
title_zh: RLVR相对于SFT在推理模型中的可证明优势：学习高效回溯
authors: "Stanley Wei, Juno Kim"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.22938v1"
tags: ["query:opd"]
score: 9.0
evidence: RLVR在推理中优势的理论分析
tldr: 大型语言模型的推理能力可通过强化微调显著提升。本文从理论上分析原因，将链式推理建模为图上的路径搜索，证明监督微调仅训练最短路径，无法学习有效回溯；而基于可验证奖励的强化学习能从死胡同高效回溯，导致两者在推理时计算量上呈指数级分离。该方法还能识别推理链中的困难决策点，从而优化计算分配，并且其推理轨迹可蒸馏至基础模型。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22938v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1566, \"height\": 513, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22938v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1465, \"height\": 414, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22938v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 840, \"height\": 589, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22938v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 821, \"height\": 533, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22938v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1761, \"height\": 704, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22938v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1753, \"height\": 970, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22938v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1745, \"height\": 1126, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22938v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1721, \"height\": 1147, \"label\": \"Figure\"}]"
motivation: 探究强化微调（RLVR）相比监督微调（SFT）在推理任务上表现更优的理论原因。
method: 将链式推理建模为图上路径搜索问题，理论分析SFT和RLVR在回溯能力上的差异。
result: 证明SFT无法学习高效回溯，RLVR能利用结果奖励实现指数级更优的推理计算效率。
conclusion: RLVR通过识别困难决策点提升计算分配，其推理轨迹可蒸馏给基础模型，具有实用价值。
---

## 摘要
近期大型语言模型（LLM）的进展表明，对预训练基础模型进行强化微调可以在推理时显著提升推理性能。本文从理论上分析了为何强化微调比纯监督微调（SFT）方法能诱导出更强的推理能力。我们将思维链（CoT）推理建模为图上的路径查找问题，并比较了流行的可验证奖励强化学习（RLVR）方法与传统的SFT。我们证明，SFT在仅使用无负例的最短路径进行训练时，无法学习如何高效回溯。相比之下，RLVR训练的模型仅利用结果奖励便能学会如何从死胡同高效回溯。这导致两种方法在推理时计算量上呈现指数级差异，并表明RLVR引导模型学习推理链中困难决策的位置，最终实现推理时计算资源的更好分配。最后，我们展示了RLVR模型的推理轨迹可被蒸馏，以训练基础模型同样高效地回溯。

## Abstract
Recent advances in large language models (LLMs) have demonstrated that reinforcement fine-tuning of pretrained base models can lead to significant gains in reasoning performance at inference time. In this work, we theoretically analyze why reinforcement fine-tuning induces better reasoning ability than purely supervised fine-tuning (SFT) methods. We model chain-of-thought (CoT) reasoning as a pathfinding problem on graphs and compare the popular method of reinforcement learning with verifiable rewards (RLVR) against traditional SFT. We prove that SFT, when trained on golden shortest paths without negative examples, fails to learn how to efficiently backtrack. In contrast, an RLVR-trained model can learn how to efficiently backtrack from dead ends using only outcome reward. This leads to an exponential separation in inference-time compute between the two methods, and demonstrates that RLVR leads the model to learn the location of difficult decisions in a reasoning chain, ultimately allowing for better allocation of inference-time compute. Finally, we show that the reasoning traces of an RLVR model can be distilled to train a base model to backtrack efficiently as well.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- 大型语言模型（LLM）通过后训练提升推理能力，主流方法包括监督微调（SFT）和基于可验证奖励的强化学习（RLVR）。实证表明 RLVR 往往比 SFT 诱导更强的推理泛化，但缺乏理论解释。
- 论文聚焦于**回溯（backtracking）**能力——当推理路径错误时，模型能否高效返回决策点并尝试其他分支。回溯被经验证明是提升 LLM 推理可靠性的关键要素。
- 研究问题：**哪种后训练方法能教会基础模型在推理时高效回溯？** 作者通过在合成图上的路径搜索任务进行理论分析。

### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：将思维链推理建模为**图上的路径查找**，设计一种“路径-星形”多图（multigraph），包含源节点、分支节点、每个分支 K 个钻石（每个钻石有 L 条多重边）、叶子节点。
- **模型架构**：使用 **bigram 模型**（线性-softmax 预测器），每个状态是带方向的边，转移概率仅依赖当前边状态。预训练阶段使其收敛到世界模型（均匀转移）。
- **SFT 训练**：仅提供从源到目标叶子的**最短路径**（无负例、无回溯），损失为交叉熵。梯度流只更新前向状态（如 R⁺, L⁺）的 logit，后向状态（R⁻, L⁻）保持不变。
- **RLVR 训练**：使用策略梯度（带结果奖励和长度惩罚），奖励函数为 \( r(x,y) = 1_{\{y \text{命中目标}\}} - \beta|y| \)，分析中取 \(\beta=1\)。采用**符号梯度流**（signed gradient flow）简化分析。理论上，RLVR 的 on-policy 探索使模型从失败分支中学习回溯信号。
- **核心理论结果**：
  - **定理2（SFT）**：SFT 后前向概率收敛到1，但后向概率固定为预训练值（\(b_j = L/(L+1), d_j=1/(L+1)\)），无法学习高效回溯。
  - **定理3（RLVR）**：RLVR 后所有前向和后向概率均收敛到1，模型学会“前向一直前进，后向一直后退”，实现高效回溯。
  - **定理4（推理时间分离）**：RLVR 模型期望命中时间为 \(\Theta(W K)\)，SFT 模型为 \(\Theta(W L^K)\)，呈指数级差距。
  - **定理5（蒸馏）**：将 RLVR 的推理轨迹蒸馏到基础 SFT 模型，可恢复 \(\Theta(W K)\) 的效率。

### 3. 实验设计：数据集、基准、对比方法
- **实验场景**：合成图路径搜索任务，参数 \(W=15, K=15, L=5\)（分支数、深度、多边数）。模型为上述 bigram 策略。
- **基准**：理论上推导的最优命中时间 \(4WK\)（对应 RLVR 收敛策略）与 SFT 收敛策略（指数时间）。
- **对比方法**：仅对比 SFT 与 RLVR 的训练动态和最终推理效率。额外实验包括使用 PPO 模拟 RLVR、单层 transformer 训练、异构图（不同分支长度）。
- **训练设置**：符号梯度下降，学习率 0.01。未使用真实数据集。

### 4. 资源与算力
- **文中未明确说明使用的 GPU 型号、数量、训练时长**。仅提及学习率等超参数，属于轻量合成实验，推测计算需求极低。
- 第6节实验“提供了示例图”，未描述硬件资源。

### 5. 实验数量与充分性
- **主要实验**：图3展示 RLVR 命中时间随迭代收敛到 \(4WK\)；图4展示各状态概率 \(a_j, b_j, c_j, d_j\) 收敛到1。
- **补充实验**（附录 E）：
  - SFT 下不同 L 和 K 的命中时间（图5左）；
  - 对后向状态进行部分监督训练以降低命中时间（图5右）；
  - PPO 模拟 RLVR（图6）；
  - 单层 transformer 训练（图7）；
  - 非对称分支长度的异构（图8）。
- **充分性评估**：作为理论研究，实验数量足够验证理论趋势，覆盖不同参数和训练算法，但未进行大规模消融或真实推理任务验证。结论基于理论推导，实验起支撑作用，客观性较好。

### 6. 论文的主要结论与发现
- **RLVR 能学会高效回溯**，而 SFT 仅能记忆前向路径，无法泛化到需要回溯的场景。
- **推理时间分离是指数级的**：RLVR 命中时间随图深度线性增长，SFT 随图深度指数增长。
- **RLVR 通过 on-policy 探索提供失败分支的梯度信号**，促使模型调整后向状态，这是 SFT 缺乏的。
- **蒸馏 RLVR 的推理轨迹可转移回溯能力**，表明回溯数据对 SFT 训练也至关重要。
- **模型能自动识别推理链中的困难决策点**，从而更好地分配推理计算资源。

### 7. 优点：方法或实验设计上的亮点
- **理论贡献突出**：首次从动力学角度形式化论证 RLVR 与 SFT 在回溯能力上的本质区别，给出严格的指数分离证明。
- **问题建模清晰**：将路径查找图与 bigram 模型结合，既保持可分析性又足够捕捉回溯行为。
- **分析工具新颖**：使用符号梯度流简化 RLVR 训练动态，将复杂的策略梯队转化为 sign ODE，便于证明收敛性。
- **实验设计简洁有力**：合成图参数可调，直接验证理论预测的收敛时间和概率，并补充 PPO 和非对称场景以展示鲁棒性。

### 8. 不足与局限
- **模型假设简化**：bigram 模型远不如实际 transformer 复杂，结论在真实 LLM 上是否完全成立仍需验证。
- **符号梯度流简化**：真实 RL 使用连续梯度更新，符号梯度流仅保留方向，可能漏掉细节。
- **仅覆盖单一图结构**：路径-星形图具有特殊对称性，结论在更复杂推理图（如环、网格）上的推广性未知。
- **缺乏真实基准实验**：未在数学题、代码生成等实际推理任务上比较 SFT 与 RLVR 的回溯能力，实验仅限合成数据。
- **未讨论多种 RL 变体**：如 PPO、GRPO 等，仅用简单策略梯度；结果对算法细节的鲁棒性未证实。
- **关于蒸馏的定理5依赖 RLVR 轨迹的可获得性**，实践中高质量轨迹可能难以获取。

（完）
