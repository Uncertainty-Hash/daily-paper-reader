---
title: Group-Graph Policy Optimization for Long-Horizon Agentic Reinforcement Learning
title_zh: 面向长视界智能体强化学习的群体图策略优化
authors: "Yunan Wang, Minghui Song, Zihan Zhang, Shaohan Huang, Haizhen Huang, Furu Wei, Weiwei Deng, Feng Sun, Qi Zhang"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.22995v1"
tags: ["query:opd"]
score: 8.0
evidence: 针对LLM的基于组的强化学习与步骤级信用分配
tldr: "现有长程智能体强化学习面临奖励稀疏、延迟及信用分配粗粒度问题。本文提出G2PO算法，将交互轨迹转化为全局状态转移图，通过分组聚合状态值估计降低方差，并基于边中心优势估计聚焦关键状态转移。在WebShop、ALFWorld和AppWorld上，G2PO相比GRPO等基线取得最高22.2%的成功率提升，验证了图结构在长程信用分配中的有效性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22995v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1436, \"height\": 689, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1435, \"height\": 688, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 393, \"height\": 422, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1447, \"height\": 410, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 555, \"height\": 385, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 699, \"height\": 320, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 728, \"height\": 322, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1449, \"height\": 277, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22995v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1442, \"height\": 832, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22995v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 590, \"height\": 323, \"label\": \"Table\"}]"
motivation: 长程智能体RL中奖励稀疏延迟，现有步级方法信用分配粗粒度且忽略状态转移的图结构，导致高方差估计和局部优化。
method: G2PO将轨迹转为全局状态图，用分组聚合状态值估计降低方差，并基于边中心优势估计通过全局TD误差标准化识别关键转移。
result: "在WebShop、ALFWorld、AppWorld上，G2PO成功率相比GRPO等基线提升最高22.2%，显著优于现有方法。"
conclusion: G2PO通过显式建模状态转移图结构，实现了细粒度全局信用分配，为长程智能体RL提供了有效范式。
---

## 摘要
基于群体的强化学习显著提升了大型语言模型在智能体场景中的表现。为了实现更细粒度的策略更新，最近的智能体强化学习框架已从轨迹级训练转向步级训练。然而，长视界智能体强化学习面临严重的奖励稀疏和延迟问题，因为反馈通常延迟数十个交互步骤。虽然现有的步级框架细化了训练粒度，但其信用分配仍然是粗粒度的，并且仍将智能体探索视为孤立的线性轨迹。这种过于简化的视角忽略了状态转移的固有图结构，导致高方差的状态值估计和短视的局部信用分配。为了克服这些关键瓶颈，我们提出了群体图策略优化（G2PO），这是一种专为多轮智能体任务设计的新型群体强化学习算法。G2PO 明确地将线性交互轨迹转换为全局状态转移图。通过聚合不同轨迹中的相同观测，我们引入了群体聚合状态值估计，从而减少了采样方差和轨迹依赖偏差。此外，我们将智能体动作重新定义为状态节点之间的转移，并提出了一种基于边的优势估计策略。通过在整个图上全局标准化时序差分误差，G2PO 明确识别并优先考虑推动绝对任务进展的关键转移。在代表性长视界基准测试——WebShop、ALFWorld 和 AppWorld 上的大量实验表明，G2PO 显著优于最先进的基于提示和强化学习的基线方法，成功率相比 GRPO 提升了高达 22.2%。

## Abstract
Group-based Reinforcement Learning (RL) has significantly enhanced Large Language Models (LLMs) in agentic scenarios. To achieve finer-grained policy updates, recent agentic RL frameworks have shifted from trajectory-level to step-level training. However, long-horizon agentic RL suffers from severe reward sparsity and delay, as feedback is often deferred for dozens of interaction steps. While existing step-level frameworks refine training granularity, their credit assignment remains coarse-grained and still treats agent exploration as isolated, linear trajectories. This oversimplified perspective ignores the inherent graph structure of state transitions, leading to high-variance state-value estimation and myopic, localized credit assignment. To overcome these critical bottlenecks, we propose Group-Graph Policy Optimization (G2PO), a novel group-based RL algorithm tailored for multi-turn agentic tasks. G2PO explicitly transforms linear interaction trajectories into a global state-transition graph. By aggregating identical observations across different trajectories, we introduce group-aggregation state-value estimation that reduces sampling variance and trajectory-dependent bias. Furthermore, we redefine agent actions as transitions between state nodes and propose an edge-centric advantage estimation strategy. By globally standardizing Temporal Difference (TD) errors across the entire graph, G2PO explicitly identifies and prioritizes critical transitions that drive absolute task progress. Extensive experiments on representative long-horizon benchmarks-WebShop, ALFWorld, and AppWorld-demonstrate that G2PO substantially outperforms state-of-the-art prompt-based and RL baselines, achieving remarkable success rate improvements of up to 22.2% over GRPO.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义（研究动机和背景）
- **问题背景**：长视界智能体强化学习（Agentic RL）中，智能体需要与环境多轮交互，反馈（奖励）通常延迟数十步才到达，导致**奖励稀疏和延迟**问题严重。  
- **现有方法局限**：  
  - 轨迹级训练（如RAGEN）随着交互步数增加，上下文长度爆炸，内存消耗大。  
  - 步级训练（如GiGPO）虽然细化了训练粒度，但信用分配仍是粗粒度的：将探索视为孤立的线性轨迹，忽略了状态转移的**图结构**。  
  - 这导致两个关键瓶颈：① **状态值估计高方差**：同一状态在不同轨迹中可能走向不同结果，单样本估计不稳定；② **局部比较范围有限**：局部组内比较无法区分“琐碎改进”与“关键突破”动作的全局重要性。  
- **研究目标**：提出一种能显式建模状态转移图结构、实现全局细粒度信用分配的强化学习算法，以提升长视界智能体任务性能。

## 2. 方法论
- **核心思想**：将多轮交互轨迹转化为**全局状态转移图**，利用图结构进行更准确、更全局的信用分配。  
- **关键技术细节**：  
  1. **状态组图构建**：  
     - 收集所有采样轨迹中的观测，将相同或相似的观测聚类为**状态组**（State Group），作为图的节点。  
     - 智能体动作作为节点间的有向边，构建完整的状态转移图。  
  2. **分组聚合状态值估计**：  
     - 对每个中间步，用折扣回报计算单步值：\(v_{ij} = \gamma^{T-j+1} R_i\)。  
     - 属于同一状态组的所有步的值取平均作为该状态组的值：\(V(G_k) = \frac{1}{|G_k|} \sum_{o_{ij} \in G_k} v_{ij}\)。  
     - **动机**：降低单轨迹随机性带来的方差，获得更稳定的状态固有值。  
  3. **多粒度优势估计**：  
     - **节点中心优势（ANC）**：在同一状态组内，比较不同动作导致的下一个状态组的值，局部归一化。  
     - **边中心优势（AEC）**：计算状态转移前后的值增益（1步TD误差），然后在整个图的全局范围内标准化，突出绝对贡献大的关键转移。  
     - **情节级优势（AEP）**：类似GRPO，基于最终奖励归一化，确保优化不偏离最终目标。  
  4. **总优势**：\(A_{ij} = A^{EP} + w \cdot (A^{NC} + A^{EC})\)，然后使用类似GRPO的裁剪代理目标函数进行策略更新。  
- **算法流程**（伪代码见附录C）：  
  - 采样N条完整轨迹 → 构造状态组图 → 分组聚合计算状态值 → 计算三种优势 → 组合优势并更新策略。

## 3. 实验设计
- **数据集/场景**：  
  - **WebShop**：模拟电商网站，需要搜索、导航、选择商品属性。  
  - **ALFWorld**：文本化的具身家居任务（如拾取、清洁、加热等6类子任务）。  
  - **AppWorld**：通过Python API交互的复杂软件生态系统任务。  
- **基准对比方法**：  
  - **前沿离线模型**：Qwen3.5-397B、DeepSeek-V3.2、Gemini-2.5-Pro。  
  - **提示方法**：ReAct、Reflexion。  
  - **RL训练方法**：PPO（带评论家）、RLOO、GRPO、GiGPO（最新步级基线）。  
- **基础模型**：Qwen2.5-1.5B-Instruct、Qwen2.5-7B-Instruct（主要），AppWorld使用Qwen2.5-14B-Instruct。  
- **训练设置**：组大小N=8，学习率1e-6，使用vLLM推理引擎，所有RL方法共享相同超参数。

## 4. 资源与算力
- **明确说明**：  
  - ALFWorld和WebShop实验：在 **4×H100 GPUs** 上运行，每实验100个迭代。  
  - AppWorld实验：在 **8×H100 GPUs** 上运行，50个迭代。  
  - 图5显示G2PO在优势计算阶段仅额外引入 **1秒** 时间开销（约占单步训练的0.4%），效率很高。

## 5. 实验数量与充分性
- **实验数量**：  
  - 三个不同场景基准测试，每个基准包含多个子任务（ALFWorld有6个子任务）。  
  - 对比了9种基线方法（包括3种离线模型、2种提示方法、4种RL方法）。  
  - 对模型大小（1.5B/7B/14B）分别评估。  
  - 消融实验：逐步添加节点中心优势、分组聚合、边中心优势，验证各组件贡献。  
  - 超参数分析（w取值0.6~1.2）。  
  - 状态值方差分布分析、推理步数效率分析。  
- **充分性与公平性**：  
  - 结果报告平均值和标准差（3个随机种子），比较可靠。  
  - 所有RL方法使用相同超参数和提示模板，确保公平。  
  - 消融实验完整，验证了每个设计的必要性。  
  - 但仅在一个框架（verl-agent）上实现，未验证其他框架下的泛化性（作者在局限中提及）。

## 6. 主要结论与发现
- **性能提升显著**：  
  - 在WebShop上成功率相比GRPO提升最多 **22.2%**，在ALFWorld提升最多 **14.4%**。  
  - 在所有基准上持续优于GiGPO、PPO、RLOO等强化学习基线。  
- **图结构有效性**：通过显式建模状态转移图，实现全局信用分配，优于局部组内比较。  
- **效率优势**：G2PO训练的模型完成所需推理步数更少，推理成本更低。  
- **计算开销极低**：额外时间成本几乎可以忽略。

## 7. 优点
- **方法创新**：  
  - 首次将交互轨迹显式转化为状态转移图，并提出分组聚合状态值估计和边中心优势估计，有效解决高方差和局部短视问题。  
  - 多粒度优势融合（情节级+步级）兼顾最终目标和过程奖励。  
- **实验设计**：覆盖多个领域和难度，基线全面，消融完整。  
- **效率**：几乎不增加额外计算成本，适合实际部署。

## 8. 不足与局限
- **实验框架局限**：仅在verl-agent框架上实现，未在更多智能体RL框架上验证（如RAGEN等）。  
- **采样方式局限**：未探索树采样、异步采样等更丰富的采样设置。  
- **状态分组依赖**：基于观测精确匹配来构建状态组，对于观测噪声或部分可观测环境可能不够鲁棒。  
- **理论假设**：分组聚合降低方差需要组大小>1，文中显示仍有约10%的状态组大小=1，这些步无法受益。  
- **应用限制**：方法需要多轨迹采样才能构建图，可能不适用于单轨迹在线场景；对内存和CPU有轻微额外需求。  
- **调参敏感性**：权重w需仔细调节（最佳值0.8），过高或过低均会降低性能。

（完）
