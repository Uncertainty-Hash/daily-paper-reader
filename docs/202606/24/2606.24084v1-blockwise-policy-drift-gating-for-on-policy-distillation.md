---
title: Blockwise Policy-Drift Gating for On-Policy Distillation
title_zh: 面向在线策略蒸馏的分块策略漂移门控
authors: "Liwen Zheng, Haiyun Jiang"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24084v1"
tags: ["query:opd"]
score: 9.0
evidence: 通过逐块策略漂移门控改进同策略蒸馏
tldr: "在线策略蒸馏中，教师信号基于学生自身采样的轨迹，但在长程推理任务中易出现策略漂移。本文提出分块策略漂移门控机制，通过计算行为学生与当前学生在采样路径上的对数概率偏移，按固定块聚合后重加权损失，无需改动教师目标或采样策略。在Qwen3数学推理基准上，64令牌分块门控将采样令牌OPD的平均pass@8从0.4978提升至0.5160。结果表明局部策略漂移是实用的控制信号，分块门控可作为提升求解率鲁棒性的简单默认方法。"
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.24084v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1353, \"height\": 413, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24084v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1272, \"height\": 456, \"label\": \"Table\"}]"
motivation: 在线策略蒸馏在长程推理任务中因策略漂移导致性能脆弱，现有修复复杂，需更轻量的漂移控制方法。
method: 计算行为学生与当前学生在采样令牌路径上的对数概率偏移，按固定块聚合后取平均并归一化，作为门控权重重加权位置损失。
result: "分块门控将采样令牌OPD在四个数学基准上的平均pass@8从0.4978提升至0.5160，优于其他变体。"
conclusion: 局部旧-当前策略漂移是实用的控制信号，分块门控可作为提升求解率鲁棒性的简单默认方法。
---

## 摘要
在线策略蒸馏（OPD）使用教师信号训练学生策略，这些信号基于学生自身采样的轨迹计算。最近的研究表明，采样令牌的OPD在长程推理任务中可能脆弱，而局部教师支持匹配是一种简单有效的修复方法。本文引入分块策略漂移门控，一种轻量级的仅学生端的旧-当前漂移控制器，用于在滚动重用下的OPD。该方法计算行为学生与当前学生在采样令牌路径上的对数概率变化，将这些变化在固定块或跨度上聚合，并使用所得分离的、均值归一化的门控重新加权OPD位置损失。它不改变教师目标、教师top-K支持或滚动策略。在一个六变体的Qwen3数学推理基准中，所有训练变体均使用统一的200步训练预算，我们以pass@8作为主要问题级解决率指标。固定64令牌块门控将采样令牌OPD在AIME24、AIME25、MATH500和AMC23上的平均pass@8从0.4978提高到0.5160。在Teacher-TopK/LSM上，Block64在训练学生中给出了最佳的四个基准平均pass@8。结果确定了局部旧-当前策略漂移作为重用OPD滚动的实用控制信号，并激励块级门控作为提高解决率鲁棒性的简单默认方法。

## Abstract
On-policy distillation (OPD) trains a student policy using teacher signals computed on trajectories sampled by the student itself. Recent work shows that sampled-token OPD can be fragile on long-horizon reasoning tasks and that local teacher-support matching is a simple and effective repair. This paper introduces blockwise policy-drift gating, a lightweight student-only old-current drift controller for OPD under rollout reuse. The method computes log-probability shifts between the behavior student and the current student on the sampled token path, aggregates these shifts over fixed blocks or spans, and uses the resulting detached, mean-normalized gates to reweight OPD position losses. It does not change teacher targets, teacher top-K supports, or the rollout policy. In a six-variant Qwen3 math reasoning benchmark with a uniform 200-step training budget for all trained variants, we use pass@8 as the primary problem-level solve-rate metric. Fixed 64-token block gating improves sampled-token OPD mean pass@8 from 0.4978 to 0.5160 across AIME24, AIME25, MATH500, and AMC23. On Teacher-TopK/LSM, Block64 gives the best four-benchmark mean pass@8 among trained students. The results identify local old-current policy drift as a practical control signal for reused OPD rollouts and motivate block-level gating as a simple default for improving solve-rate robustness.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在线策略蒸馏（On-Policy Distillation, OPD）在长程推理任务中，教师信号基于学生自身采样的轨迹计算，但训练过程中存在“策略漂移”问题——当同一学生轨迹在多个优化轮次中被重用时，当前学生与生成该轨迹的行为学生之间会发生策略偏移，导致部分位置的监督信号过时或不可靠。
- **背景与动机**：现有研究表明，采样令牌级别的OPD在长程推理中脆弱，局部教师支持匹配（Teacher-TopK/LSM）是一种修复方法。然而，该工作关注的是教师可靠性的问题，而非学生自身策略漂移。作者指出，在PPO式训练中，滚动重用时旧-当前策略偏移是一个潜在的控制信号，但尚未被充分研究。本文希望探索：这种**局部旧-当前策略漂移**是否应被用来调节OPD损失权重？以及何种聚合粒度最有效？
- **整体含义**：本文提出一种轻量级的、仅作用于学生端的**分块策略漂移门控（Blockwise Policy-Drift Gating）**，作为对现有OPD损失的插件式权重，在不改变教师目标、教师支持分布或滚动策略的前提下，提升问题级解决率（pass@8）的鲁棒性。

## 2. 方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：在滚动重用的训练过程中，计算行为学生（旧策略）与当前学生（新策略）在采样路径上每个令牌位置的对数概率变化量（漂移），将这些变化量按块或跨度聚合，得到归一化的门控权重，用于重加权各位置上的OPD损失，使损失更多地关注漂移较小的区域（即更“新鲜”的轨迹段），减少因策略漂移造成的噪声信号影响。

- **关键技术细节**：
  - **定义漂移**：对于采样响应中的令牌位置 \( (i,t) \)，定义旧-当前学生对数概率变化量：
    \[
    d_{i,t} = \log \pi_{\theta}(y_{i,t} \mid x_i, y_{i,<t}) - \log \pi_{\text{old}}(y_{i,t} \mid x_i, y_{i,<t})
    \]
    其中 \(\pi_{\theta}\) 是当前学生策略，\(\pi_{\text{old}}\) 是行为学生策略（即生成该轨迹时的策略）。
  - **门控形状**：使用 soft 漂移门控：\( g = \exp(-\tau |s|) \)，其中 \(s\) 是聚合后的漂移得分，\(\tau=1\)。随后进行均值归一化：
    \[
    \bar{g} = \frac{\sum_{i,t} m_{i,t} g_{i,t}}{\sum_{i,t} m_{i,t} + \epsilon}, \quad \tilde{g}_{i,t} = \text{stopgrad}\left( \frac{g_{i,t}}{\bar{g} + \epsilon} \right)
    \]
    归一化后门控可以大于1，目的是保持平均损失规模不变，只改变相对权重。
  - **聚合粒度**：
    - **Block64-Gate**：将响应序列划分为固定64令牌的块，块内对 \(d_{i,t}\) 取平均，将该块的门控广播到块内所有令牌。
    - **NewlineSpan-Gate**：以换行符为界划分跨度，合并短跨度、分割长跨度，无换行符时回退到固定块。
  - **最终损失函数**：
    \[
    \mathcal{L}_{\text{gate}} = \frac{\sum_{i,t} m_{i,t} \tilde{g}_{i,t} \ell^{\text{base}}_{i,t}}{\sum_{i,t} m_{i,t} + \epsilon}
    \]
    其中 \(\ell^{\text{base}}_{i,t}\) 是基准OPD损失（如采样令牌损失或Teacher-TopK/LSM损失），分母仍使用原始响应掩码。门控通过 stopgrad 切断反向传播，仅作为权重，不成为额外优化目标。

- **算法流程**（文字说明）：
  1. 学生策略采样一批轨迹。
  2. 在每次策略更新前，重新计算当前学生对采样响应的对数概率，并与预存的行为学生对数概率对比，得到每个令牌的漂移 \(d_{i,t}\)。
  3. 按选定粒度（如64令牌块）聚合漂移得分，计算门控值并归一化。
  4. 将归一化门控作为权重乘到已有的OPD位置损失上，进行梯度更新。

## 3. 实验设计

- **数据集与基准**：
  - **训练数据**：来自 DAPO-Math-17k 数学语料库，共约 179 万行（row count，非唯一提示数）。
  - **评估基准（Benchmark）**：四个数学推理测试集——**MATH500**、**AIME 2024**、**MathArena AIME 2025**、**AMC23**（AMC23来自2023年AMC 12A/12B，共40题）。
  - **主指标**：**pass@8**（每个问题采样8个生成，任一正确即视为解决），同时辅助报告 avg@8（样本级平均准确率）。
- **对比方法**：共6个训练变体：
  - Sampled-token OPD（无门控）
  - OPD + Block64 gate
  - OPD + NewlineSpan gate
  - Teacher-TopK/LSM（无门控）
  - LSM + Block64 gate
  - LSM + NewlineSpan gate
- **基线**：基础学生（未训练）和教师参考（4B模型）。

## 4. 资源与算力

- **模型**：学生为 **Qwen3-1.7B-Base**，教师为 **Qwen3-4B-Base-GRPO**（经过GRPO数学推理后训练）。
- **硬件**：单次训练使用 **A800 80GB GPU**，训练批大小为4（由于显存和墙钟时间限制，但保留了滚动组大小和响应长度）。
- **训练预算**：所有变体统一使用 **200步（200 steps）** 训练，固定预算，不进行早停。
- **未提及细节**：未说明具体GPU数量（可能为单卡或少量卡），也未报告训练总时长。

## 5. 实验数量与充分性

- **实验数量**：在6个方法变体 × 4个评估集上报告 pass@8，共24个主要结果。另提供了 avg@8 作为辅助对照。
- **消融实验**：仅对比了两种聚合粒度（Block64 vs NewlineSpan），没有包括令牌级别门控、序列级别门控、裁剪重要性权重等基线（作者在局限性中承认缺失这些对照）。
- **充分性评价**：
  - **优势**：固定200步预算保证了方法间公平对比，未进行超参数挑选；使用 pass@8 作为主指标，符合长程推理评估惯例。
  - **不足**：仅使用一个学生模型（1.7B）、一个教师模型（4B）、一个数学训练环境、无重复随机种子。AIME25 样本量较小，结果需谨慎解读。缺少对块大小的系统扫描（如32、64、128）、未分析门控分布统计量（如对数比率百分位数、有效令牌比率等）。总体实验规模有限，属于初步研究。

## 6. 论文的主要结论与发现

- **主要发现**：
  - **Block64门控提升采样令牌OPD**：在四个基准上平均 pass@8 从 0.4978 提高到 0.5160（约+1.8个百分点）。
  - **LSM+Block64给出最佳聚合pass@8**：达到 53.3%，优于所有其他学生变体。
  - NewlineSpan 门控也接近但不如 Block64 一致。
  - 平均样本准确率（avg@8）与 pass@8 趋势一致，表明门控主要提高了求解率鲁棒性而非单纯扩展稀有样本覆盖。
- **核心结论**：局部旧-当前策略漂移是实用的控制信号，**64令牌固定块门控**可作为提高 OPD 求解率鲁棒性的简单默认方法。该方法正交于教师支持修复（如LSM），可以叠加使用。

## 7. 优点

- **简单轻量**：仅需计算当前与行为学生的对数概率差，无额外模型或教师调用，不改变训练结构。
- **即插即用**：可直接应用于现有的 OPD 或 LSM 损失上，无需修改目标函数或采样策略。
- **实验公平**：固定200步训练预算、统一评估设置，避免了早停或额外调参的影响。
- **关注实际指标**：以问题级 pass@8 作为主指标，而非似然或困惑度，更贴近应用场景。
- **明确描述局限性**：作者诚实地指出了缺少多种基线控制、未做随机化种子实验等问题。

## 8. 不足与局限

- **实验覆盖不完整**：仅有一个学生模型（1.7B）和一个教师模型（4B），未测试更大或更小的模型。未使用多个随机种子，可能引入随机偏差。
- **缺失关键消融实验**：没有与**令牌级门控**、**序列级门控**、**裁剪重要性权重（Token-IS）** 等基线直接对比，无法论证64令牌块是否最优。也未系统扫描块大小（如32、64、128）。
- **AIME25样本量小**：AIME25 只有约30题，差异可能不显著。
- **门控设计缺陷**：对块内漂移取平均可能正负抵消，即使局部变化很大也可能得到小门控。作者承认应探索均值绝对值或RMS等替代。
- **缺乏诊断分析**：未报告门控分布、有效令牌比例、梯度范数尾部等内部状态，方法的工作机制理解有限。
- **单向偏移问题**：门控仅降低漂移大的位置权重，但未考虑漂移方向（正或负）可能包含信息。
- **不适用于异步设置**：本文仅在同步滚动重用下测试，未与异步OPD或新鲜度控制框架对比。

（完）
