---
title: "H$^2$SD: Hybrid Hindsight Self-Distillation"
title_zh: H²SD：混合事后自我蒸馏
authors: "Qiye Cai, Yichuan Ma, Linyang Li, Peiji Li, Yongkang Chen, Qipeng Guo, Yicheng Zou, Tao Gui, Xiaocheng Feng, Bing Qin"
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.18955v1"
tags: ["query:opd"]
score: 9.0
evidence: 用于大语言模型推理的同策略自蒸馏方法
tldr: 强化学习可验证奖励在LLM推理任务中提供稀疏监督，限制信用分配。现有方法如OPD需额外教师且可能信息泄漏，RLSD缺乏纠正方向。本文提出H^2SD混合后见自蒸馏框架，根据轨迹正确性差异化使用教师信号：成功轨迹调整幅度，失败轨迹提供纠正方向。在多个推理基准上，H^2SD显著优于RLVR、OPSD和RLSD基线，同时保持稳定优化和生成效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.18955v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 725, \"height\": 509, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18955v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1555, \"height\": 634, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18955v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 739, \"height\": 563, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18955v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 735, \"height\": 315, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18955v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1523, \"height\": 478, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.18955v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1726, \"height\": 543, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18955v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 853, \"height\": 369, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18955v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 849, \"height\": 385, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18955v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 849, \"height\": 283, \"label\": \"Table\"}]"
motivation: 现有RLVR奖励稀疏，OPD需额外教师且易信息泄漏，RLSD缺乏显式纠正方向。
method: 对成功轨迹用教师调整更新幅度；对失败轨迹用教师参考提示并最小化反向KL散度。
result: 在多个推理基准上持续优于RLVR、OPSD、RLSD基线，且训练稳定、生成高效。
conclusion: H^2SD通过按轨迹正确性差异化利用教师信号，有效平衡稀疏奖励与方向纠正。
---

## 摘要
基于可验证奖励的强化学习（RLVR）大幅提升了大型语言模型在数学推理、代码生成等任务上的推理能力。然而，大多数 RLVR 方法为整个轨迹分配一个标量结果奖励，导致监督稀疏且 token 级别的信用分配有限。在线策略蒸馏（OPD）通过从更强的教师模型蒸馏 token 级别分布来提供更密集的监督，但需要额外的教师，并且通常假设共享词汇表。在线策略自我蒸馏（OPSD）通过将同一模型基于特权信息进行条件化来构建教师策略，从而消除了这一依赖。然而，直接匹配教师分布可能导致信息泄露和不稳定的优化。RLSD 通过仅使用教师信号来调节更新幅度，避免了直接匹配，但当采样的推理失败时，它无法提供明确的纠正方向。为了解决这一权衡，我们提出了 H²SD，一种混合事后自我蒸馏框架，它根据轨迹的正确性以不同方式使用教师。对于成功的轨迹，教师接收被确认为正确的学生响应以及重述指令，并利用其对原始响应 token 的概率来调节更新幅度，而不改变由奖励确定的方向。对于失败的轨迹，我们基于包含关键推理步骤和已验证答案的参考提示来条件化教师，并最小化学生到教师的反向 KL 散度。在多个具有挑战性的推理基准上的实验表明，H²SD 持续优于代表性的 RLVR、OPSD 和 RLSD 基线，同时保持稳定的优化和良好的生成效率。

## Abstract
Reinforcement learning with verifiable rewards (RLVR) has substantially improved the reasoning capabilities of large language models on tasks such as mathematical reasoning and code generation. However, most RLVR methods assign a scalar outcome reward to an entire trajectory, resulting in sparse supervision and limited token-level credit assignment. On-policy distillation (OPD) provides denser supervision by distilling token-level distributions from a stronger teacher model, but requires an additional teacher and typically assumes a shared vocabulary. On-policy self-distillation (OPSD) removes this dependency by conditioning the same model on privileged information to construct a teacher policy. However, directly matching the teacher distribution may cause information leakage and unstable optimization. RLSD avoids direct matching by using the teacher signal only to modulate update magnitudes, but it cannot provide an explicit correction direction when the sampled reasoning fails. To address this tradeoff, we introduce $\mathrm{H}^{2}\mathrm{SD}$, a hybrid hindsight self distillation framework that uses the teacher differently according to trajectory correctness. For successful trajectories, the teacher receives the student response confirmed as correct together with a rephrasing instruction, and its probabilities on the original response tokens are used to modulate update magnitudes without changing the direction determined by the reward. For failed trajectories, we condition the teacher on a reference hint containing key reasoning steps and a verified answer, and minimize the reverse KL divergence from the student to the teacher. Experiments on multiple challenging reasoning benchmarks show that H$^2$SD consistently outperforms representative RLVR, OPSD, and RLSD baselines while maintaining stable optimization and favorable generation efficiency.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：强化学习可验证奖励（RLVR）在大型语言模型推理中表现优异，但仅提供稀疏的序列级标量奖励，导致 token 级别信用分配不足，难以定位正确或错误 token。
- **现有方法的局限性**：
    - 在线策略蒸馏（OPD）需要额外的强教师模型，且通常要求师生共享词表，限制应用。
    - 在线策略自我蒸馏（OPSD）利用同一模型基于特权信息作为教师，但直接匹配教师分布可能引起信息泄露（学生隐式学习到特权信息）和训练不稳定。
    - RLSD 仅用教师信号调节更新幅度，避免了直接匹配和泄露，但无法提供显式的纠正方向，尤其在失败轨迹中仅惩罚错误 token 而不指示正确方向。
- **整体含义**：论文旨在设计一种混合策略，根据轨迹正确性差异化利用教师信号，在成功轨迹上进行精细化幅度调整，在失败轨迹上提供方向性纠正，从而平衡稀疏奖励与稳定优化。

### 2. 论文提出的方法论

- **核心思想**：提出 **H²SD（Hybrid Hindsight Self-Distillation）**，一种基于轨迹正确性的路由框架。
- **关键技术细节**：
    - **Hint 生成**：使用外部分布更强的 LLM（如 Kimi-K2.6）为每个问题生成自然语言提示（hint），包含关键推理步骤和经验证的最终答案。该提示作为特权信息仅用于训练阶段，不直接作为教师，且无需与当前模型共享词表。
    - **成功轨迹处理**：
        - 教师接收学生已验证正确的响应，并附加一条重述指令（Rephrase instruction），输出基于原始响应 token 的概率。
        - 仅使用这些概率计算 RLSD 中的幅度权重 \( w_t = \exp( \text{sign}(A) \cdot \Delta_t ) \)，其中 \(\Delta_t = \log P_T(y_t) - \log P_S(y_t)\)，不改变由奖励确定的优化方向。
        - 最终 token 级优势 \( \hat{A}_t = A \cdot [(1-\lambda) + \lambda \cdot \text{clip}(w_t, 1-\epsilon_w, 1+\epsilon_w)] \)。
    - **失败轨迹处理**：
        - 教师基于 hint（参考提示）和问题上下文产生分布 \( p_T^{h} \)。
        - 学生最小化反向 KL 散度 \( \mathcal{L}_{\text{RKL}} = \frac{1}{T} \sum_t D_{\text{KL}}(p_S^t \parallel \text{sg}[p_T^{h,t}]) \)，其中 sg 表示停止梯度，防止教师分布被学生反向作用。
    - **总体目标**：
        \[
        \mathcal{L}_{\text{H^2SD}} = \mathbb{E}_{x,y} \left[ m \cdot \mathcal{L}_{\text{RLSD}} + \gamma (1-m) \cdot \mathcal{L}_{\text{RKL}} \right]
        \]
        其中 \( m = 1 \) 表示轨迹正确，\( \gamma \) 控制失败轨迹上的蒸馏强度。

### 3. 实验设计

- **数据集/场景**：三个具有挑战性的逻辑推理基准：
    - **Sudoku**：6×6 和 8×8 网格填充。
    - **Calcudoku**：5×5 和 6×6 网格，含区域级算术运算约束。
    - **Arrow Maze**：6×6 到 10×10 五种尺寸，要求箭头分配满足射线长度约束。
- **Benchmark**：各任务按尺寸分组，报告 pass@1 准确率；总平均为各任务平均值的整体平均。
- **对比方法**：
    - Base LLM（Qwen3-30B-A3B-Instruct-2507）
    - GRPO（经典 RLVR）
    - SDPO（Self-Distillation Policy Optimization，使用同批次正确响应作为教师）
    - RLSD（仅幅度调节）
    - OPSD（直接匹配教师分布）
    - SRPO（Sample Routing Policy Optimization，成功轨迹用 GRPO，失败用自蒸馏）
    - RLSD+hint（将 RLSD 中的特权信息从答案换成 hint，隔离提示本身的效果）
- **公平性**：所有方法的基座模型相同；H²SD 与 OPSD、RLSD+hint 使用完全相同的 hint，保证结论归因于学习目标而非提示信息。

### 4. 资源与算力

- **硬件配置**：训练使用 **4 个节点**，每个节点配备 **8 张 NVIDIA H200 140GB GPU**（共 32 张 GPU）。
- **软件框架**：基于 VERL 和 SGLang 实现。
- **未明确说明**：训练步数、总时长、单次推理成本等细节未给出。

### 5. 实验数量与充分性

- **实验组数**：覆盖 3 个基准共 10 个任务尺寸（Sudoku 2 种、Calcudoku 2 种、Arrow Maze 5 种），主表（表1）包含 8 种方法对比。
- **消融实验**：
    - **路由策略**（表2）：对比仅幅度、仅反向KL、反向路由、H²SD。
    - **特权信息类型**（表3）：对比程序化反馈、仅正确答案、兄弟方案、外部提示。
    - **重述效果**（表4、图5）：在幅度、反向KL、H²SD 中对比加/不加重述。
    - **熵动态分析**（图4）：H²SD vs. 反向路由的熵变化。
- **充分性与客观性**：
    - 消融设计完整，能够分离各组件贡献。
    - 所有对比实验在相同 hint 上比较学习目标（表1中RLSD+hint 与 OPSD、H²SD 用同一 hint），公平地证明了 H²SD 的收益来自蒸馏方式而非 hint 本身。
    - 缺点：未在数学推理（如 GSM8K、MATH）或代码生成等 RLVR 典型任务上验证；仅依赖逻辑推理基准。

### 6. 论文的主要结论与发现

- H²SD 在所有 10 个任务尺寸的平均准确率上取得最好或第二好结果，整体平均 50.49%，远超次优（RLSD+hint 24.72%）。
- 成功/失败路由是关键技术：交换路由（成功用反向KL、失败用幅度）导致熵快速坍塌和性能崩溃（表2、图4）。
- 提示质量至关重要：包含过程级信息的外部提示（hint）远优于仅正确答案或程序化反馈（表3）。
- 对成功轨迹的重述仅在幅度调节中有益，在反向KL中反而有害（表4），支持 H²SD 的混合设计。
- H²SD 在生成更少 token 的条件下取得更高准确率，说明其高效利用学习信号（图3）。

### 7. 优点

- **创新性混合框架**：巧妙利用轨迹正确性区分两种监督模式，既保留了 RLSD 的稳定性，又提供了失败时的方向纠正。
- **避免额外教师依赖**：外部强模型仅用于生成 hint，不参与蒸馏过程，且无需与训练模型共享词表。
- **信息泄露抑制**：成功轨迹中教师仅用于幅度调节（停止梯度），失败轨迹中教师分布也停止梯度，防止特权信息渗入学生。
- **实操友好**：使用与 OPSD 相同的提示生成成本，但训练更稳定且效果更好。
- **泛化到未见尺寸**：在 Arrow Maze 中，模型在训练未见的 8×8、10×10 尺寸上同样领先，说明学到了通用推理能力。

### 8. 不足与局限

- **实验覆盖狭窄**：仅在三个逻辑推理领域（数独、计算数独、箭头迷宫）验证，未涉及更广泛的 RLVR 任务（如数学竞赛题、代码生成），结论泛化性需进一步证实。
- **外部提示依赖**：核心依赖更强的模型（Kimi-K2.6）生成 hint，若提示质量差或分布不匹配，可能引入偏差；是否所有场景都能获得优质提示是实际部署的潜在瓶颈。
- **算力要求高**：32 张 H200 GPU 的训练规模较大，资源受限实验室可能难以复现。
- **未对比最新方法**：如 DAPO、GSPO、GATES 等，仅与较早期的 OPSD、RLSD 对比，最先进基线竞争性不足。
- **超参数敏感性**：成功/失败阈值（\( m=1 \) 定义）、蒸馏强度 \( \gamma \)、幅度上下界等未做系统敏感性分析，可能影响可复现性。

（完）
