---
title: "LenGuard-GPC: Length Guarding with Guided-Prompt Consistency for Spatial Reasoning Reinforce Learning"
title_zh: "LenGuard-GPC: 基于引导提示一致性的长度守卫用于空间推理强化学习"
authors: "Xingjian Tao, Yiwei Wang, Yujun Cai, Jing Tang"
date: 2026-07-19
pdf: "https://arxiv.org/pdf/2607.17243v1"
tags: ["query:opd"]
score: 8.0
evidence: 利用可验证奖励的强化学习、GRPO、为轨迹提供密集奖励反馈
tldr: 多视角空间推理中，链式推理易冗长且不准确。提出LenGuard-GPC框架，利用引导提示一致性计算KL散度作为密集奖励，并引入分段长度奖励控制推理长度。在六个基准上，该方法比标准GRPO提升准确率并减少平均响应长度。为空间推理强化学习提供了有效的长度控制和密集奖励机制。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17243v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1681, \"height\": 761, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17243v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1088, \"height\": 469, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.17243v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1711, \"height\": 978, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17243v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1710, \"height\": 841, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17243v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1707, \"height\": 291, \"label\": \"Table\"}]"
motivation: 标准GRPO仅提供稀疏结果级奖励，无法定位推理错误和控制响应长度。
method: 通过比较标准与引导提示的token级分布KL散度作为密集奖励，并引入分段长度奖励平衡长度与质量。
result: 在六个多视图空间推理基准上，准确率提升，平均响应长度减少。
conclusion: 密度奖励与长度控制结合有效提升多视角空间推理的准确性和效率。
---

## 摘要
多视图空间推理需要视觉语言模型跨图像比较视觉证据、对齐对象对应关系，并在长视觉上下文中推断空间关系，这种情况下链式思考推理往往变得冗长而不更准确。带可验证奖励的强化学习自然适合此任务，但标准GRPO奖励依赖稀疏的结果级反馈，不提供关于推理轨迹何处出错的信号，也不控制其长度。我们提出LenGuard-GPC，一个密集奖励框架，同时解决这两个问题。对于每个采样轨迹，它比较标准提示和引导提示下的token级预测分布，并使用得到的token和KL散度作为密集奖励信号。由于这种KL惩罚逐token累积，否则无论质量如何都会奖励较短的响应，我们引入分阶段长度奖励，将推理长度控制在受控范围内，而不简单地鼓励简洁。在六个多视图空间推理基准上，LenGuard-GPC比普通GRPO提高了准确性，同时减少了平均响应长度。

## Abstract
Multi-view spatial reasoning requires vision-language models to compare visual evidence across images, align object correspondences, and infer spatial relations over long visual contexts, a setting where chain-of-thought reasoning tends to grow verbose without becoming more accurate. Reinforcement learning with verifiable rewards is a natural fit for this task, but standard GRPO reward relies on sparse outcome-level feedback and gives no signal about where a reasoning trajectory goes wrong, nor any control over its length. We propose LenGuard-GPC, a dense reward framework that addresses both problems together. For each sampled trajectory, it compares the token-wise predictive distributions under a standard prompt and a guided prompt, and uses the resulting token-sum KL divergence as a dense reward signal. Since this KL penalty accumulates over tokens and would otherwise reward shorter responses regardless of their quality, we introduce a staged length bonus that keeps reasoning length within a controlled range without simply encouraging brevity. On six multi-view spatial reasoning benchmarks, LenGuard-GPC improves accuracy over vanilla GRPO while reducing average response length.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义

- **研究动机**：多视图空间推理任务要求视觉语言模型（VLM）跨图像建立物体对应关系并推断空间关系。在此过程中，链式推理（CoT）往往冗长且不准确：模型倾向于列举每个视图中的无关细节，而忽略了关键的跨视图对应步骤。标准强化学习方法（如GRPO）仅依赖稀疏的结果级奖励（最终答案是否正确），无法定位推理链条中何处出错，也无法控制响应长度。
- **整体含义**：论文指出多视图空间推理中推理质量与长度并非正相关，关键在于是否有效解决了跨视图对应。因此需要一种既提供密集过程级反馈、又能合理控制推理长度的训练框架。

## 2. 方法论：LenGuard-GPC

- **核心思想**：利用**引导提示一致性**（Guided-Prompt Consistency）作为密集奖励信号，同时引入**分段长度守卫**（Staged Length Guard）避免模型倾向过短或过长的推理。
- **关键技术细节**：
  - **引导提示一致性奖励**：
    - 对每个训练实例，构造标准提示 \(x = (I, q)\) 和引导提示 \(x_{gp} = (I, q, h)\)，其中 \(h\) 是由GPT生成的单句指导。
    - 模型从标准提示采样一个推理轨迹 \(y = (y_1, ..., y_T)\)。
    - 使用教师强制分别计算在该轨迹下标准提示和引导提示的token级预测分布：\(p_t^{std}\) 和 \(p_t^{gp}\)。
    - 计算每个token的KL散度 \(D_t = D_{KL}(p_t^{std} \| p_t^{gp})\)，并累加得到总散度 \(S_{KL}(y) = \sum_t D_t\)。
    - 奖励定义为负累加散度：\(r_{KL}(y) = -S_{KL}(y)\)。轨迹越接近引导提示行为，惩罚越小。
  - **分段长度守卫**：
    - 定义长度边界 \(l_1=160, l_2=360, l_3=768\) 和奖励水平 \(r_1=0.25, r_2=0.30\)。
    - 奖励函数 \(r_{len}(T)\)：当 \(T < l_1\) 时为0；在 \([l_1, l_2]\) 内从 \(r_1\) 线性增加到 \(r_2\)；在 \([l_2, l_3]\) 保持 \(r_2\)；超过 \(l_3\) 不再有额外奖励。
    - 该设计使KL奖励（鼓励简洁）与长度奖励（防止过短）动态平衡。
  - **总奖励**：
    - \(r(y) = \lambda_{task} r_{task}(y) - \lambda_{KL} S_{KL}(y) + \lambda_{len} r_{len}(T)\)，其中 \(r_{task}\) 包含准确率奖励和格式奖励。
  - **训练流程（算法1）**：
    1. 对每个实例构造标准提示和引导提示。
    2. 从标准提示采样G个轨迹。
    3. 对每个轨迹计算所有奖励分量并合并。
    4. 在组内归一化奖励得到优势，用GRPO更新策略。
  - **推理阶段无需引导提示**，仅采样标准提示。

## 3. 实验设计

- **训练数据**：来自 VST-500K 的多视图空间推理子集，约13000个实例，每个实例含多张不同视角图像。
- **评估基准（6个）**：
  - **领域内（多视图空间推理）**：MMSI-Bench、MindCube（仅评估，未使用其训练集）、ViewSpatial-Bench。
  - **领域外**：3DSR（单图像空间推理）、BLINK（多图像但非主要空间推理）、CV-Bench（单图像空间推理）。
- **对比方法**：
  - 闭源模型：Gemini-2.5-Pro、GPT-5、Gemini-3-Pro-Preview。
  - 开源通用模型：InternVL3（2B/8B）、Qwen2.5-VL（3B/7B）、Qwen3-VL（2B/4B/8B）等。
  - 空间智能模型：SpatialLadder、SpaceR、ViLaSR、Cambrian-S、VST（3B/7B）等。
  - 基线：基础模型 Qwen3-VL-8B-Instruct、普通GRPO、LenGuard-GPC（本方法）。
- **超参数**：基础模型 Qwen3-VL-8B-Instruct，学习率 1e-6，\(\lambda_{acc}\)（准确率奖励系数）文中隐含，格式奖励为0.2，\(\lambda_{KL}=5\times10^{-3}\)，\(\lambda_{len}\) 由分段长度奖励定义直接给出。

## 4. 资源与算力

- 论文**未明确说明**使用的 GPU 型号、数量、训练时长等具体算力信息。仅提到训练过程在 Qwen3-VL-8B-Instruct 上进行，引导提示评估增加了约1.6倍的训练时间（由于计算KL散度，但轨迹只采样一次，且教师强制可并行计算）。

## 5. 实验数量与充分性

- **实验数量**：
  - 主实验结果：在6个基准上报告了准确率（表1），覆盖领域内/外，包括11个对比模型。
  - 细粒度分析：在 MMSI-Bench 上分解了11个子类别（表2）。
  - 消融实验：在 MMSI-Bench 上比较了有无分段长度奖励（表3）——实际上表3对比了普通GRPO、无分段长度奖励、完整方法；另外文中提到“Free format Reasoning + RL”“w/o GRPO”“w/o Format Reward”等消融，但只在文字描述中出现，未展示数据表。
  - Token效率分析：在 MMSI 和 ViewSpatial 上比较了平均生成token数（图2）。
- **充分性评价**：
  - 主实验覆盖多样化的基准和模型，对比全面。
  - 消融实验仅在一个基准（MMSI）上进行，且部分消融条件缺少表格数据，稍显不足。
  - 缺少对不同长度边界超参数（\(l_1,l_2,l_3\)）的敏感性分析。
  - 引导提示由GPT-5生成，未探讨不同来源引导提示的影响。
  - 总体实验设计较充分，但消融和鲁棒性分析可更深入。

## 6. 主要结论与发现

- LenGuard-GPC 在6个基准上平均准确率57.3%，优于普通GRPO（55.3%）和基础模型（51.4%），尤其在多视图空间推理领域内基准（MMSI +3.9点、MindCube +3.7点）提升显著。
- 在领域外基准（3DSR、BLINK、CV-Bench）上也取得一致但较小的增益，表明方法不会过拟合训练分布。
- 细粒度分析显示主要改进来自跨视图位置推理（Cam.–Obj. +13.9点, Cam.–Reg. +12.0点）。
- 在 MMSI 上平均响应长度减少14.9%，在 ViewSpatial 上减少38.1%，表明更紧凑的CoT。
- 消融验证了分段长度奖励的必要性：无该奖励时准确率从36.1%降至33.5%。

## 7. 优点

- **方法创新性**：将引导提示一致性作为RL奖励（而非监督损失），为长上下文多模态推理提供密集过程反馈，且推理时无额外开销。
- **长度控制**：分段长度奖励避免了简单惩罚或鼓励长/短响应，实现了简洁与充分的平衡。
- **通用性**：框架可扩展到其他长上下文多模态任务（如视频理解、多图像比较等）。
- **实验全面**：包含领域内/外基准、细粒度分析、消融和效率分析，对比了多个强基线。
- **结果提升显著**：在关键基准上准确率提升超过5个点，同时缩短响应，证明方法有效且高效。

## 8. 不足与局限

- **算力资源未披露**：无法评估训练成本或可复现性。
- **消融实验有限**：仅在MMSI上做完整消融，且缺乏对长度边界超参数的敏感性测试；引导提示来源单一（均由GPT-5生成）。
- **领域外增益较小**：在3DSR、BLINK等任务上提升微弱（<1.5点），表明方法在非多视图空间推理任务上的优势有限。
- **未与最新的密集奖励方法对比**：如分步骤奖励、过程监督等，对比基线仅为普通GRPO。
- **潜在偏差**：引导提示依赖外部模型（GPT-5），可能引入不一致或偏见；训练数据仅来自VST-500K的子集，覆盖广度有限。
- **应用限制**：需要为每个训练实例构造引导提示，增加了数据准备成本；且需要存储token级分布，带来额外内存开销。

（完）
