---
title: "CRAFT: Counterfactual Credit Assignment from Free Sibling Rollouts for Self-Distilled Agentic Reinforcement Learning"
title_zh: CRAFT：用于自蒸馏代理强化学习的基于免费兄弟轨迹的反事实信用分配
authors: "Zibin Meng, Kani Chen"
date: 2026-06-28
pdf: "https://arxiv.org/pdf/2606.29476v1"
tags: ["query:opd"]
score: 9.0
evidence: 利用兄弟轨迹的反事实信用分配进行自蒸馏强化学习
tldr: 自蒸馏智能体强化学习常用门控机制限制信用分配。CRAFT提出三支柱方案：反事实信用重用GRPO的兄弟轨迹计算符号化每token信用；非对称控制器动态平衡蒸馏与KL权重；逐token极化KL惩罚。各支柱可独立关闭确保位精确复现。在多个环境和模型规模下验证，反事实贡献带来显著提升。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.29476v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 859, \"height\": 280}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.29476v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 881, \"height\": 385}, {\"url\": \"assets/tables/arxiv/2606.29476v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 881, \"height\": 370}, {\"url\": \"assets/tables/arxiv/2606.29476v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1539, \"height\": 455}, {\"url\": \"assets/tables/arxiv/2606.29476v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 872, \"height\": 263}, {\"url\": \"assets/tables/arxiv/2606.29476v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 865, \"height\": 393}, {\"url\": \"assets/tables/arxiv/2606.29476v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 883, \"height\": 1029}, {\"url\": \"assets/tables/arxiv/2606.29476v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 818, \"height\": 918}]"
motivation: 现有门控信号仅回顾已实现轨迹且无符号，无法反映反事实影响，限制信用分配效果。
method: 三支柱：反事实token重要性（重用兄弟轨迹计算符号化信用）、非对称控制器（动态调节权重）、逐token极化KL惩罚（根据信用符号切换模式）。
result: 在三个智能体环境、四种模型规模及多项基线中验证，反事实贡献独立于实现漂移带来性能增益。
conclusion: CRAFT通过反事实信用分配克服现有局限，提供可复现的改进，提升自蒸馏强化学习效果。
---

## 摘要
自蒸馏代理强化学习通过令牌级蒸馏损失增强轨迹级奖励，其教师使用相同策略但以特权上下文为条件。现有方法使用单一标量——师生对数概率差距——来门控该损失。该信号具有双重局限性：它是回顾性的，仅对实际发生的轨迹评分，从不考虑反事实轨迹；同时它缺乏符号意识，当教师偏好的行动可能损害轨迹时无法发出信号。我们提出CRAFT，一种三支柱信用分配方案，解决了这两个局限性。支柱1：反事实令牌重要性，重用GRPO已经采样的G-1个兄弟轨迹，并通过对数概率差距对其进行重要性加权，形成自我归一化的组级反事实优势变化估计，该变化源自每步对教师偏好行动的上加权；这以接近零的额外计算产生带符号的每令牌信用。支柱2是一个非对称控制器，它沿着门控活动的指数移动平均线降低参考KL权重时提高蒸馏权重，反之亦然。支柱3按令牌极化KL惩罚，根据信用的符号在寻求模式和覆盖模式更新之间切换。每个支柱都有一个独立开关，当禁用时，使得损失和梯度在IEEE-754算术下与基线逐字节相同，因此任何测量到的增益均可归因于算法变化而非实现漂移。我们证明了估计量的一致性和方差界，给出了结构和逐位精确的可复现性保证，并在三种代理环境、四种模型规模和五种端到端方法以及两个表列先前工作基线上评估了CRAFT。其中包括Adaptive-CRINGE，一种与CRAFT共享支柱2的比较器，从而隔离出反事实贡献。

## Abstract
Self-distilled agentic reinforcement learning augments trajectory-level reward with a token-level distillation loss, using as its teacher the same policy conditioned on privileged context. The prevailing recipe gates this loss by a single scalar, the teacher-student log-probability gap. This signal is doubly limited: it is retrospective, scoring only the realised rollout and never the counterfactual ones, and it is sign-blind, never signalling when a teacher-preferred action would have harmed the trajectory. We introduce CRAFT, a three-pillar credit-assignment scheme that addresses both limitations. Pillar 1, Counterfactual Token Importance, reuses the G-1 sibling rollouts that GRPO already samples and importance-weights them by the log-probability gap to form a self-normalised estimate of the group-level counterfactual change in advantage from up-weighting teacher-preferred actions at each step; this yields a signed per-token credit at near-zero extra compute. Pillar 2 is an asymmetric controller that raises the distillation weight as it lowers the reference-KL weight along an exponential moving average of gate activity, and conversely. Pillar 3 polarises the KL penalty token by token, switching between a mode-seeking and a mode-covering update according to the sign of the credit. Each pillar has an independent switch that, when disabled, renders the loss and gradient byte-identical to the baseline in IEEE-754 arithmetic, so any measured gain is attributable to algorithmic change rather than implementation drift. We prove the estimator's consistency and a variance bound, give structural and bit-exact reproducibility guarantees, and evaluate CRAFT across three agentic environments, four model scales, and five end-to-end methods, plus two tabulated prior-work baselines. Among these is Adaptive-CRINGE, a comparator sharing Pillar 2 with CRAFT, isolating the counterfactual contribution.

---

## 论文详细总结（自动生成）

# CRAFT: 反事实信用分配论文详细总结

## 1. 核心问题与整体含义
- **研究背景**：自蒸馏代理强化学习（Self-Distilled Agentic RL）在轨迹级奖励上叠加令牌级蒸馏损失，使用同一策略在特权上下文下的输出来充当教师。现有方法使用单一标量门控（teacher-student log-prob gap Δ_t）来控制蒸馏。
- **问题**：该门控信号存在**双重局限性**：
  - **回顾性**：仅基于已实现的轨迹评分，无法考虑反事实轨迹（如果选择教师动作会怎样）。
  - **符号盲**：没有符号区分，即使教师偏好动作可能损害轨迹，门控仍为正，无法发出“不要跟随”信号。
- **整体含义**：需要一种能利用已有计算资源（GRPO的兄弟轨迹）来估计反事实信用、并带有符号的每令牌信用分配方案，以提升自蒸馏强化学习的有效性。

## 2. 方法论
### 核心思想
- 将现有单门控（SDAR）替换为三支柱信用分配方案，三个支柱可独立开关，关闭时损失和梯度与基线逐字节相同（IEEE-754），确保增益源于算法改进而非实现漂移。

### 关键技术细节
- **支柱1：反事实令牌重要性（CTI）**
  - 利用GRPO每组中G-1个兄弟轨迹（已采样），通过重要性加权（权重 ∝ exp(Δ_t)，Δ_t为师生对数概率差距）估计组级反事实优势变化：
    \[
    \widehat{CTI}_t^{(i)} = \sum_{j \neq i} w_t^{(j)} A^{(j)} - A^{(i)}, \quad w_t^{(j)} \propto \exp(\Delta_t^{(j)})
    \]
  - 通过中心化sigmoid映射为带符号信用 c_t ∈ (-1,1)，梯度停止。
  - CTI损失：\( L_{CTI}(\theta) = - \text{Agg}[ c_t \cdot \log \pi_\theta(y_t | s_t) ] \)（REINFORCE风格）。
  - 计算成本极低：仅一次组级softmax和加权和，无额外模型推理。

- **支柱2：阶段感知自适应控制器**
  - 定义门控活动比率 ρ_gate = 有效令牌中|c_t|>θ_g的比例。
  - 维护指数移动平均 ρ_EMA。
  - 根据ρ_EMA动态调节两个系数：蒸馏权重λ(t)与ρ_EMA正相关，参考KL权重μ(t)负相关（反方向移动）。
  - 预热期n_warmup内保持初始值，防止早期噪声。

- **支柱3：方向极化KL惩罚**
  - 根据c_t符号选择不同KL项：
    - c_t > θ_KL：模式寻求（reverse KL，拉向π_ref）
    - c_t < -θ_KL：模式覆盖（forward KL变体，推离π_ref）
    - |c_t| ≤ θ_KL：标准KL（k3）
  - 负分支使用符号翻转的k1形式，是有界梯度规则，非无偏KL估计。

- **总损失**：
  \[
  L_{CRAFT} = L_{PG} + \mu(t) L_{KL\text{-}pol} + \lambda(t) L_{CTI}
  \]
  其中L_PG为GRPO的PPO裁剪目标，保持不变。

### 理论保证
- **Prop.1**：CTI估计量是自归一化重要性采样的一致估计量，组大小G→∞时收敛于组级目标，偏差O(1/G)。
- **Prop.2**：方差界 \( \text{Var}(\widehat{CTI}) \leq R_{\max}^2 \bar{\rho}_t^2 / (G-1) \)。
- **P3-P7**：包括退化性、控制器不动点、极化KL一致性、代理有效性、位精确复现性。

## 3. 实验设计
### 数据集/场景
- **三个多轮代理RL基准**：
  - **ALFWorld**：文本化具身环境（成功率为指标）。
  - **Search-QA**（来自Search-R1）：直接检索变体（精确匹配为指标）。
  - **WebShop**：程序化验证器环境（归一化0-100奖励）。

### Benchmark与对比方法
- **模型规模**：两种家族共4个模型：Qwen3-1.7B、Qwen2.5-3B、Qwen2.5-7B、Qwen3-8B。
- **端到端方法**（共5种）：
  - GRPO（无蒸馏）
  - RLSD
  - SDAR（单门控基线）
  - **Adaptive-CRINGE**：复用CRAFT的Pillar2控制器，但信用来自CRINGE负项，用于隔离P1+P3贡献。
  - **CRAFT-Full**（提案方法）
- **先验工作表列基线**：GRPO+OPSD、Skill-SD（来自SDAR论文）。

### 实验族
- **主网格（E1）**：4模型×3环境×7方法，结果见表2。
- **逐支柱消融（E2）**：SDAR + P1 / P1+P2 / P1+P3 及完整CRAFT，见表3。另有**反事实消融（A6）**：设G_min=999强制退化到单轨迹代理，**KL方向交换（A7）**：符号翻转作为证伪测试。
- **超参数消融（E3）**：扫描β±和τ。
- **OOD和随机检索鲁棒性（E7/E8）**：在Qwen2.5-3B上测试30%训练集外模板和随机检索特权上下文。

## 4. 资源与算力
- **硬件配置**：
  - 小模型（1.7B/3B）：8×24GB GPU。
  - 大模型（7B/8B）：4×48GB GPU。
- **训练细节**：每配置150步/单元格，GRPO组大小G=8。未明确报告总GPU小时或训练时长，仅提及按照SDAR设置。
- **复现保证**：87个测试的回归套件（包括4个逐位精确测试通过torch.equal验证P7，运行约4秒在CPU上）。

## 5. 实验数量与充分性
- **实验数量**：
  - 主网格12个（模型×环境）细胞，每个7种方法（包括两个表列基线），共84个数据点。
  - 消融实验（表3）：8行，覆盖P1/P2/P3组合、反事实消融、KL方向交换。
  - 超参数消融（A4-A5）：扫描β和τ。
  - OOD和随机检索（表4）：两个应力测试。
  - 此外有代码回归测试（87个测试）。
- **充分性与公平性**：
  - **客观性**：所有CRAFT方法使用相同超参数（默认值列于附录C），未针对特定环境调参。
  - **公平性**：CRAFT所有支柱具有独立开关，关闭时与基线逐位相同（P7），确保增益来自算法而非实现漂移。
  - **严格比较器**：Adaptive-CRINGE共享Pillar2，直接隔离反事实贡献（P1+P3），避免“只是自适应KL”或“只是CRINGE”的质疑。
  - 消融设计合理：A6（反事实消融）和A7（KL方向交换）作为证伪测试，结果符合预期。
  - 多模型规模（1.7B到8B）验证可扩展性。

## 6. 主要结论与发现
- **CRAFT-Full在所有（环境，模型）单元格中均为最佳方法**（表2）。
- **关键对比**：CRAFT-Full > Adaptive-CRINGE 的增益为正（例如Qwen3-1.7B上ALFWorld +2.6，Search-QA +3.1等），表明**反事实贡献（P1+P3）独立于Pillar2控制器有效**。
- **规模缩放模式**：小模型增益更大（Qwen3-1.7B提升+5.5 WebShop），大模型增益缩小但仍为正（7B/8B约1.6-2.1），符合预测。
- **消融分析**（表3）：
  - +P1单独即带来大部分增益（+1.9~+2.0%），确认Pillar1是实质性贡献者。
  - +P1+P2或+P1+P3增量小（≤0.4%），其中Pillar3主要是完备性贡献。
  - 反事实消融（A6）增益回落至SDAR基线（相差≤0.2点），KL方向交换（A7）表现低于SDAR，验证了理论方向。
- **鲁棒性**：CRAFT具有最小的分布内→OOD下降（平均6.2 vs SDAR的9.5），且在随机检索下下降适度（78.5 vs SDAR的72.0），符合预测。

## 7. 优点
- **方法创新**：
  - 首次利用GRPO免费兄弟轨迹进行反事实信用分配，零额外采样成本。
  - 自归一化重要性采样估计量具有一致性和方差界。
  - 三支柱设计各可独立开关，便于消融和复现。
  - 位精确复现性（P7）确保实验可归因性，是方法论亮点。
- **实验设计亮点**：
  - 设置**Adaptive-CRINGE**严格比较器，隔离反事实贡献，避免常见混淆。
  - 多模型规模、多环境、多基线全面评估。
  - 包括OOD和随机检索鲁棒性应力测试。
  - 消融实验中包含证伪测试（A6、A7），强化因果推理。
- **理论贡献**：给出了CTI估计量一致性、方差界、控制器不动点、代理有效性等理论分析。

## 8. 不足与局限
- **实验覆盖**：
  - 所有基准均为**文本化代理环境**，未涉及多模态或真实网站任务。OOD分析仅限于同一领域内的模板分布，跨领域泛化未验证。
  - 仅评估了模型规模从1.7B到8B，未测试更大模型（如13B+），且仅两个模型家族（Qwen2.5和Qwen3），泛化性有限。
- **方法局限**：
  - **Pillar1依赖组卷**（G≥2），非组骨干方法（如PPO单轨迹）需通过合成兄弟轨迹适配，未来工作。
  - **引入了9个新超参数**（β±, τ, G_min, θ_g, [λ,μ]范围, α, n_warmup, θ_KL），虽有默认值但调优负担增加。
  - **Pillar3独立增益很小**（约0.3-0.5%），主要是完备性贡献，且负分支使用的是有偏更新规则而非无偏KL估计（附录B承认）。
  - **Pillar1组级估计与每轨迹反事实的差距**（交换性假设E）仅在t=0精确成立，下游退化，虽然有G_min=999消融控制，但直接校准方法未提供。
- **偏差风险**：论文未报告多次重复实验的方差或置信区间，仅报告单次最终检查点结果，可能存在随机波动。代码中无随机种子固定保证的明确说明（除了回归测试）。
- **应用限制**：需要特权上下文s+（如技能标注、验证器提示），缺乏此类语境时无法应用自蒸馏部分。

（完）
