---
title: "KL for a KL: On-Policy Distillation with Control Variate Baseline"
title_zh: KL对KL：带控制变量基线的同策略蒸馏
authors: "Minjae Oh, Sangjun Song, Gyubin Choi, Yunho Choi, Yohan Jo"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07865v1"
tags: ["query:opd"]
score: 9.0
evidence: 提出带控制变量基线的vOPD以稳定在线策略蒸馏
tldr: 在线策略蒸馏（OPD）因其单样本蒙特卡洛估计器的高梯度方差导致训练不稳定。本文提出vOPD，将OPD形式化为策略梯度强化学习，并引入控制变量基线（值函数）来降低方差。该值函数存在闭式解，即每个token上学生与教师之间的负反向KL散度，可从已有前向传播直接获得，无需额外网络或推理。在数学和科学推理基准上，vOPD显著优于原始OPD，且性能匹配昂贵的全词汇基线，同时保持轻量级计算。通过原理性的RL方差缩减，vOPD实现了高效稳定的蒸馏。
source: arxiv
selection_source: fresh_fetch
motivation: OPD中单样本梯度估计方差高，导致训练不稳定，现有全词汇基线开销大或top-k近似有偏。
method: 将OPD转为策略梯度RL，引入控制变量基线（值函数），其闭式解为每token负反向KL，无需额外网络，仅使用单样本估计并减去基线以保留无偏梯度。
result: 在数学和科学推理基准上，vOPD优于原始OPD，匹配全词汇基线性能，且top-k近似基线可进一步降低成本而不损失效果。
conclusion: vOPD通过RL方差缩减提供了高效稳定的在线策略蒸馏，适用于大语言模型推理训练。
---

## 摘要
同策略蒸馏（OPD）已成为大型语言模型（尤其是在推理领域）的一种主流训练后范式。然而，由于其单样本蒙特卡洛估计器的梯度方差较大，OPD在实践中仍不稳定，且稳定训练的方案尚不成熟。我们提出了vOPD（带控制变量基线的同策略蒸馏），该方法将OPD视为策略梯度强化学习，并通过引入来自强化学习文献的控制变量基线（典型如价值函数）来稳定训练。我们证明了OPD的价值函数存在闭式形式，即学生与教师之间每个token的负反向KL散度，可直接从已计算的前向传播中获取，无需额外的评论家网络或推理。现有的稳定方法要么计算整个词汇表上的完整token级反向KL，增加显著开销，要么将其限制在top-k支持集上，从而引入偏差。vOPD保留了轻量级的单样本估计器，将价值函数作为分离基线减去，在降低方差的同时保持梯度无偏。此外，我们证明基线的top-k近似可进一步降低成本而不影响性能。在数学和科学推理基准测试中，vOPD始终优于原始OPD，并与最昂贵的全词汇基线性能相当，通过基于强化学习的方差缩减实现了同策略蒸馏的高效稳定化。

## Abstract
On-Policy Distillation (OPD) has emerged as a dominant post-training paradigm for large language models, especially for reasoning domains. However, OPD remains unstable in practice due to the high gradient variance of its single-sample Monte Carlo estimator, and recipes for stable training are still immature. We propose vOPD (On-Policy Distillation with a control variate baseline), which casts OPD as policy-gradient RL and stabilizes it by introducing a control variate baseline-canonically a value function -- from the RL literature. We show that the OPD value function admits a closed form as the per-token negative reverse KL divergence between the student and the teacher, available directly from the already-computed forward pass with no additional critic or inference. Existing stabilization methods either compute the full token-level reverse KL over the entire vocabulary, adding significant overhead, or restrict it to a top-k support, biasing the objective. vOPD instead preserves the lightweight single-sample estimator, subtracting the value function as a detached baseline to keep the gradient unbiased while reducing variance. Furthermore, we show that a top-k approximation of the baseline further lowers cost without compromising performance. Across mathematical and scientific reasoning benchmarks, vOPD consistently outperforms vanilla OPD and matches the most expensive full-vocabulary baseline, offering an efficient stabilization of On-Policy Distillation through principled RL variance reduction.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

在线策略蒸馏（On-Policy Distillation, OPD）已成为大语言模型（LLM）后训练（尤其是推理任务）的主流方法，它通过最小化学生与教师之间的反向KL散度来提供密集的token级监督信号，相比基于稀疏奖励的RLVR方法更高效。然而，OPD在实践中存在严重的不稳定性，根源在于其单样本蒙特卡洛估计器的梯度方差过高。现有稳定手段要么计算全词汇表上的完整KL（OPD full-V），带来巨大计算开销；要么将KL限制在top-k支持集（OPD top-k），引入梯度偏差且性能增益有限。本文提出vOPD（带控制变量基线的OPD），利用强化学习中的控制变量基线来无偏地降低方差，同时保持单样本估计器的轻量计算。

### 2. 论文提出的方法论：核心思想、关键技术细节

**核心思想**：将OPD重新解释为策略梯度强化学习，其中每步奖励为 \( r_t = \log \pi_T(y_t|c_t) - \log \pi_\theta(y_t|c_t) \)。标准CV基线是价值函数 \( V^{\pi_\theta}(c_t) = \mathbb{E}_{y_t\sim\pi_\theta}[r_t] \)。作者发现对于OPD，该价值函数存在闭式解：  
\[ V^{\pi_\theta}(c_t) = -D_{KL}(\pi_\theta(\cdot|c_t) \| \pi_T(\cdot|c_t)) \]  
即负的每步反向KL散度，可直接从已有前向传播中计算，无需额外评论家网络或推理。

**技术细节**：
- **vOPD full-V**：在OPD的单样本梯度估计中减去该闭式基线，得到优势 \( a_t = r_t + D_{KL}(\pi_\theta\|\pi_T) \)。基线仅在前向计算，不反向传播，因此反向传播仅作用于单采样token，与原始OPD一致，保持梯度无偏。
- **vOPD top-k**：为进一步降低计算成本，基线中的KL近似为学生top-k token上的KL（重新归一化）。由于基线不依赖于采样token \( y_t \)，该近似仍保持梯度无偏（不同于OPD top-k直接改变目标导致有偏）。实验表明即使k=5也足够有效。
- **方差缩减分析**：在高不匹配token（学生与教师强烈分歧）处，KL值大，基线带来的正偏移大，从而压缩负奖励的长尾，降低梯度方差。

### 3. 实验设计：数据集、基准、对比方法

**数据集与基准**：
- 数学推理：MATH500、Minerva Math、AMC23、AIME24/25。报告avg@n和pass@n（n=8或32）。
- 科学推理：SciKnowEval（化学子集）、GPQA-Diamond。

**对比方法**：
- 基线：Student（未训练）、OPD（基础版）、OPD top-k（k=20）、OPD full-V（全词汇）。
- 提出方法：vOPD top-k（默认k=20）、vOPD full-V。

**模型配置**（三个主要设置 + 一个尺寸不匹配 + 一个模型家族）：
1. Qwen3-1.7B → Qwen3-1.7B-Base
2. Qwen3-4B → Qwen3-4B-Base
3. Olmo-3-7B-Think → Olmo-3-7B-Base
4. Qwen3-1.7B → Qwen3-0.6B-Base（补充）
5. 科学推理：Qwen3-1.7B → Qwen3-1.7B-Base

训练：DAPO-Math-17K（14K样本，1 epoch）用于数学；SciKnowEval化学子集（1890样本，10 epoch）用于科学。采用LoRA微调。

### 4. 资源与算力

- **硬件**：单个 NVIDIA H200 NVL 或 A100 NVL GPU，搭配 Intel Xeon Gold 6530/6230 CPU。
- **训练时长**：2小时（1.7B on A100）至 6小时（7B on H200）。所有训练在单GPU上完成。
- 论文未明确提及总GPU小时数或大规模扩展实验，但给出了各规模下的每步墙钟时间对比（图3），显示vOPD top-k近乎与OPD一样快，而vOPD full-V只有少量额外开销。

### 5. 实验数量与充分性

**实验数量**：
- 数学推理：4个benchmark × 3个主要模型配置（1.7B, 4B, 7B）+ 1个尺寸不匹配配置（0.6B），共约15组对比。
- 科学推理：2个benchmark × 1个配置。
- 消融实验：k值（5,20,50,100）对性能的影响；基线预测误差（MSE）分析；梯度范数随训练步数的变化；奖励vs优势分布可视化。
- 墙钟时间比较：两种模型规模（1.7B, 4B）下的5种方法。

**充分性与公平性**：
- 对比方法全面，包括有偏的top-k、昂贵的full-V以及原始OPD。
- 使用了不同模型家族（Qwen3, Olmo-3）和不同规模（0.6B至7B），覆盖了同尺寸和尺寸不匹配情况。
- 评价指标包括avg@n和pass@n，统计充分。
- 消融实验验证了top-k基线近似的有偏性与有效性，并展示了超参数鲁棒性。
- 不足：实验仅限于7B及以下模型，未验证更大规模（如70B）。未在更多领域（如编程、对话）测试。

### 6. 论文的主要结论与发现

1. **vOPD显著提升OPD稳定性与性能**：在所有模型配置和基准上，vOPD（full-V和top-k）平均abs增益超过3%，最高+6.2%（MATH500）。尤其vOPD top-k与vOPD full-V性能接近，说明粗粒度近似已足够。
2. **优于现有稳定化方法**：vOPD显著超过OPD和OPD top-k，与昂贵的OPD full-V性能相当或更优，但计算成本大幅降低（墙钟时间最高减少57.7%）。
3. **方差压缩机制**：vOPD通过减去KL基线，将高负奖励（高不匹配token）压缩，降低梯度方差；梯度范数验证其稳定效果（比OPD低1-2个数量级）。
4. **泛化能力**：除数学外，在科学推理（SciKnowEval, GPQA-Diamond）上表现一致。
5. **超参数鲁棒**：top-k 从5到100均有效且稳定，说明即便粗略估计价值函数也能带来显著方差缩减。

### 7. 优点

- **理论严谨**：基于强化学习控制变量基线，推导出闭式价值函数，保证无偏且可计算。
- **计算高效**：无需额外网络模型，不增加反向传播计算量（vOPD full-V仅增加前向KL计算；vOPD top-k进一步降低）。
- **即插即用**：可无缝替代现有OPD实现，无需修改训练框架。
- **实验充分**：涵盖多个模型家族、规模、基准、消融和可视化分析，验证了方法的普适性和稳定性。
- **可解释性强**：通过奖励vs优势分布、梯度范数等分析，揭示了vOPD选择性压制噪声梯度的机理。

### 8. 不足与局限

- **规模限制**：实验局限于7B及以下模型，未探索更大规模（如70B、130B）下的效果和计算开销。
- **依赖强教师**：vOPD作为蒸馏方法需访问一个更强的教师模型，无法用于自蒸馏或无教师场景。
- **仅考虑token级KL**：工作聚焦于token级反向KL（OPD常用形式），未探索序列级KL或其他散度（如前向KL）下的适用性。
- **墙钟时间优化不完善**：vOPD top-k在论文实现中尚未比vOPD full-V快很多（图3中两者接近），作者指出未来可优化。
- **科学推理任务较单一**：仅使用了化学子集和GPQA，未涵盖更多科学领域或编程任务，可能限制通用性结论。
- **未进行多重随机试验**：未报告多次运行的标准差或统计显著性检验，单次运行结果可能存在随机性。

（完）
