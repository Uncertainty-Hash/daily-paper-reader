---
title: "A Brief Overview: On-Policy Self-Distillation In Large Language Models"
title_zh: 简评：大语言模型中的同策略自蒸馏
authors: "Fangming Cui, Sunan Li, Jiahong Li"
date: 2026-05-18
pdf: "https://arxiv.org/pdf/2605.18141v2"
tags: ["query:opd"]
score: 9.0
evidence: 大语言模型中在线策略自蒸馏的概述
tldr: "传统知识蒸馏依赖外部教师模型且存在分布不匹配。On-Policy Self-Distillation (OPSD) 让单个大型语言模型同时作为教师和学生，教师有权访问验证推理轨迹，学生仅见问题，通过最小化两者逐token分布差异训练。该方法消除了外部教师，直接利用真实解决方案，并解决了分布不匹配问题。相比标准On-Policy Distillation，GPU内存消耗减少40%-60%，为LLM高效自蒸馏提供了统一框架。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.18141v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1323, \"height\": 1437, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.18141v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1573, \"height\": 261, \"label\": \"Table\"}]"
motivation: 传统蒸馏依赖外部教师且存在分布不匹配，需要更高效利用真实解决方案的自蒸馏方法。
method: 单个LLM同时扮演教师和学生，教师获取验证推理轨迹，学生仅见问题，通过最小化逐token分布差异训练。
result: "无需外部教师，解决分布不匹配，GPU内存减少40%-60%。"
conclusion: OPSD为LLM提供高效自蒸馏框架，简化训练流程并提升内存效率。
---

## 摘要
同策略自蒸馏（On-Policy Self-Distillation，OPSD）是一种统一的学习框架，其中单个大语言模型同时扮演教师和学生。与依赖独立且通常更大的教师模型的传统知识蒸馏不同，OPSD在不同上下文角色下运行：教师策略被赋予对验证推理轨迹的优先访问权，而学生策略仅观察问题陈述。OPSD通过最小化学生自身采样轨迹上两个角色之间的逐令牌分布散度进行训练，从而将其自身推理行为与知晓解决方案的合理化过程对齐。OPSD消除了对外部教师的需求，直接利用真实解信息，并解决了异策略蒸馏中固有的分布不匹配问题。与标准同策略蒸馏（OPD）相比，OPSD通常将GPU内存消耗降低约40%-60%。本文对近期大语言模型OPSD进展背后的概念基础、方法论创新和原则性设计进行了简要分析。本讨论从该领域初学者的视角出发，旨在为同样刚接触该领域的研究者提供OPSD设计原则和新兴模式的简明概述。

## Abstract
On-Policy Self-Distillation (OPSD) is a unified learning framework in which a single large language model acts simultaneously as both teacher and student. Unlike conventional knowledge distillation that relies on a separate, often larger teacher model, OPSD operates under different contextual roles: the teacher policy is granted privileged access to verified reasoning traces, while the student policy observes only the problem statement. OPSD is trained to minimize per-token distributional divergence between the two roles over trajectories sampled from the student itself, thereby aligning its own reasoning behavior with solution-aware rationalizations. OPSD eliminates the need for an external teacher, directly leverages ground-truth solution information, and resolves the distribution mismatch inherent in off-policy distillation. OPSD typically reduces GPU memory consumption by approximately 40%-60% compared to standard On-Policy Distillation (OPD). In this paper, we present a brief analysis of the conceptual foundations, methodological innovations, and principled designs underlying recent advances in OPSD for large language models. This discussion, crafted from the perspective of beginners in this field, aims to provide a concise overview of the design principles and emerging patterns of OPSD in LLMs, intended for researchers who are similarly new to this area.

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义（研究动机和背景）

- **背景**：大语言模型（LLM）的后训练通常采用“三板斧”——监督微调（SFT）、强化学习（RL，如GRPO）和同策略蒸馏（OPD）。SFT存在分布不匹配和暴露偏差；RL奖励稀疏且存在信用分配问题；OPD依赖更强外部教师，导致内存开销大且存在能力差距。
- **核心问题**：如何在不依赖外部教师的前提下，实现同策略（on-policy）的密集逐token监督，同时避免分布不匹配？
- **整体含义**：On-Policy Self-Distillation（OPSD）通过让同一个模型同时扮演教师和学生角色，利用特权信息（真实答案或推理轨迹）打破对称性，解决了上述矛盾。它是一种统一学习框架，消除了对外部教师的需求，直接利用真实解信息，并显著降低GPU内存消耗（40%-60%）。

### 2. 方法论

- **核心思想**：单一LLM共享参数，但根据不同上下文条件扮演两个角色：学生仅见问题 \(x\)，教师额外见真实答案 \(y^*\)（特权信息）。学生生成轨迹 \(\hat{y}\)，教师提供逐token概率分布，学生最小化两者分布散度。
- **关键技术细节**：
  - 损失函数（式6）：
    \[
    \mathcal{L}_{\text{OPSD}}(\theta) = \mathbb{E}_{(x,y^*)\sim\mathcal{D},\,\hat{y}\sim\pi^\text{stud}_\theta(\cdot|x)} \left[ \sum_{t=1}^{|\hat{y}|} \mathrm{KL}\left( \pi^\text{stud}_\theta(\cdot|x,\hat{y}_{<t}) \,\big\|\, \pi^\text{teach}_\theta(\cdot|x,y^*,\hat{y}_{<t}) \right) \right]
    \]
  - 除KL外，推荐使用广义Jensen-Shannon散度（JSD\(_\beta\)，式7-9），具有对称、有界、可调（在正向KL与反向KL之间平滑插值）等优点。
  - 训练流程：从数据集中采样\((x, y^*)\) → 学生根据\(x\)采样回答\(\hat{y}\) → 教师根据\(x, y^*\)和\(\hat{y}_{<t}\)计算分布 → 用JSD或KL最小化两者差异 → 更新共享参数\(\theta\)。
- **关键设计**：教师和学生共享权重，但通过不同上下文区分，使模型学会让“盲”学生行为模仿“知情”教师行为。

### 3. 实验设计

- **基准数据集（来自论文引用）**：
  - **数学推理**：GSM8K、MATH、AIME、OpenThoughts
  - **代码生成**：HumanEval、MBPP、LiveCodeBench
  - **代理交互环境**：WebShop、ALFWorld
  - **通用安全对齐**：UltraFeedback、BeaverTails
  - **视觉多模态**：COCO、MMMU
- **对比方法**：文中表1对比了SFT、GRPO、OPD、OPSD的监督粒度、关键优势和弱点，但未列出具体数值结果。
- **实验性质**：本文为综述性论文，未执行自有实验，而是引用现有工作（如RLSD、SRPO、GEAR等）来展示OPSD各种设计变体的有效性。因此没有统一的Benchmark对比表。

### 4. 资源与算力

- 文中明确提到“允许在8张A100 GPU上进行实验，这可以覆盖大多数场景”。
- OPSD相比标准OPD通常减少GPU内存消耗约40%-60%。
- 未提及具体训练时长或硬件型号细节（如A100 80GB等），也未提供某个具体实验的算力报告。

### 5. 实验数量与充分性

- **实验数量**：由于是简短综述，论文本身没有进行任何控制实验或消融实验。实验证据完全依赖于引用的第三方工作（约60篇参考文献）。
- **充分性**：作为一篇概述，它较为系统地梳理了OPSD的8个设计方向（混合策略优化、反馈驱动蒸馏、训练稳定性校准、推理压缩加速、能力内化迁移、视觉多模态蒸馏、结构化轨迹蒸馏、基础范式理论），覆盖面较广。但未提供任何复现或对比实验，因此不能直接证明自身结论的客观性和公平性。读者需要查阅原始文献以验证性能提升。

### 6. 主要结论与发现

- OPSD成功消除了对外部教师模型的依赖，同时保留了同策略密集监督的优点。
- 相比OPD，GPU内存降低40%-60%，使其在资源受限或极端大规模模型场景下成为唯一可行方案。
- OPSD可与RLVR、反馈机制、自适应教师暴露等技术结合，形成丰富的设计空间（如Hybrid Policy Optimization、Feedback-Driven Distillation等）。
- 自蒸馏可能带来信号退化或过度自信风险，可通过AntiSD、CREDIT等校准方法缓解。

### 7. 优点

- **方法创新**：利用同一模型的双角色设计，理念简洁高效。
- **资源友好**：大幅度降低内存占用，适合研究机构和企业快速尝试。
- **框架统一**：将多个后训练范式（SFT、RL、蒸馏）的优势融合，实现了同策略+密集监督+无外部教师。
- **设计多样性**：文中详细分类了8种设计子方向，展示了OPSD的广泛应用潜力（推理、代码、安全、多模态、压缩等）。

### 8. 不足与局限

- **缺乏实证验证**：作为一篇概述，论文没有提供任何新实验结果或消融分析，无法独立评估OPSD的实际效果。
- **比较不充分**：仅用文字定性对比不同方法，缺少定量基准和误差分析。
- **特权信息处理挑战**：文中提到“需要小心处理特权信息”，但未给出具体解决方案；OPSD假设真实答案已知，这在许多真实场景中不可得。
- **退化风险**：自教师可能导致“自我确认偏差”，虽然引用了AntiSD等工作，但未提出通用防御策略。
- **综述范围限制**：仅聚焦于OPSD，未与最新RL方法（如DPO、GRPO其他变体）进行深度对比。
- **可复现性低**：未提供代码、超参数或训练细节，仅给出参考文献。

（完）
