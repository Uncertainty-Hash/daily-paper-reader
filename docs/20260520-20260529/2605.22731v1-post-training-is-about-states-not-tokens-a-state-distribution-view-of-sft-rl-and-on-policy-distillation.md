---
title: "Post-Training is About States, Not Tokens: A State Distribution View of SFT, RL, and On-Policy Distillation"
title_zh: 后训练的关键在于状态，而非词元：从状态分布视角看SFT、RL和在线蒸馏
authors: Dong Nie
date: 2026-05-21
pdf: "https://arxiv.org/pdf/2605.22731v1"
tags: ["query:opd"]
score: 9.0
evidence: 针对LLM中SFT、RL和在线策略蒸馏的状态分布视角
tldr: 大语言模型后训练常通过损失函数分析，但忽略了状态分布的关键作用。本文形式化后训练为状态分布塑造，比较SFT、RL和on-policy distillation在固定状态与当前策略诱导状态下的效果。实验发现轻度SFT提升GSM8K且少遗忘，压力SFT导致遗忘；OPD从降质教师出发超越教师；轻量RL提升GSM8K并保持记忆。研究表明训练状态的来源与局部性同监督信号一样重要。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.22731v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1100, \"height\": 304, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.22731v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1448, \"height\": 343, \"label\": \"Table\"}]"
motivation: 现有后训练分析聚焦损失函数，但忽略了状态分布（prompt+生成前缀）对训练效果的影响，尤其在遗忘和泛化方面。
method: 形式化后训练为状态分布塑造，在Qwen3-0.6B-Base上对比SFT、RL、on-policy distillation在固定状态与动态状态下的表现。
result: 轻度SFT提升GSM8K且遗忘少；OPD从劣质教师超越教师；轻量RL提升GSM8K并保持TruthfulQA和MMLU性能。
conclusion: 训练状态的来源和局部性对后训练效果至关重要，比监督信号形式更具影响力。
---

## 摘要
大语言模型的后训练方法，如监督微调（SFT）、强化学习（RL）和蒸馏，通常通过其损失函数进行分析：最大似然、策略梯度、前向KL、反向KL或相关的目标级变体。我们研究了一个互补因素：施加监督的状态分布。对于自回归策略，一个状态是提示加上已生成的前缀。SFT在固定数据集的状态上训练，而RL和在线蒸馏（OPD）在当前学习器诱导的状态上训练。我们将后训练形式化为状态分布塑造，并使用Qwen3-0.6B-Base在GSM8K上运行受控的小规模研究，同时用TruthfulQA和MMLU作为保留评估。我们的结果显示了三个现象。第一，温和的SFT运行在几乎不遗忘的情况下提升了GSM8K，而压力SFT运行导致了显著的保留损失。第二，来自退化SFT教师的OPD在GSM8K、TruthfulQA和MMLU上超越了该教师，尽管仅以该教师为唯一监督源。第三，轻量级的在线RL运行在保持保留的同时提升了GSM8K。这些结果支持了一种以状态为中心的后训练观点：训练状态的来源和局部性与监督信号的形式同样重要。

## Abstract
Large language model post-training methods such as supervised fine-tuning (SFT), reinforcement learning (RL), and distillation are often analyzed through their loss functions: maximum likelihood, policy gradients, forward KL, reverse KL, or related objective-level variants. We study a complementary factor: the state distribution on which supervision is applied. For an autoregressive policy, a state is a prompt plus generated prefix. SFT trains on fixed dataset states, while RL and on-policy distillation (OPD) train on states induced by the current learner. We formalize post-training as state-distribution shaping and run a controlled smallscale study using Qwen3-0.6B-Base on GSM8K, with TruthfulQA and MMLU as retention evaluations. Our results show three phenomena. First, a mild SFT run improves GSM8K with little forgetting, while a stress SFT run causes substantial retention loss. Second, OPD from a degraded SFT teacher surpasses that teacher on GSM8K, TruthfulQA, and MMLU, despite using the teacher as its only supervision source. Third, a lightweight on-policy RL run improves GSM8K while preserving retention. These results support a state-centric view of post-training: the source and locality of training states can be as important as the form of the supervision signal.

---

## 论文详细总结（自动生成）

# 论文中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：大语言模型后训练方法（SFT、RL、蒸馏）通常通过损失函数（最大似然、策略梯度、KL散度等）来分析和对比，但这一视角忽略了另一个关键因素：**训练时监督信号应用在什么样的状态（state）上**。在自回归模型中，状态指的是“提示（prompt）+已生成前缀（generated prefix）”。不同的后训练方法可能使用截然不同的状态分布（固定数据集 vs. 当前策略诱导的状态），而这会显著影响模型的泛化、遗忘和局部改进能力。
- **整体含义**：本文主张后训练的本质是“状态分布塑造”，而不仅仅是“词元监督”；**训练状态的来源和局部性（是否来自学习器自身）** 与监督信号的形式同样重要，甚至更大程度上决定了训练行为的稳定性和效果。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：将后训练分解为两个独立轴：**状态源（state source）** 和 **信号源（signal source）**。状态源决定更新在哪些上下文中发生，信号源决定提供什么目标（词元、奖励、分布）。
- **形式化框架**：后训练步骤可写为三步操作：
  1. 从状态分布 \(q_k(s)\) 采样状态 \(s\)；
  2. 从信号提供者 \(S(s)\) 获取监督对象 \(z\)（如词元、连续文本、奖励、分布）；
  3. 更新策略 \(\theta_{k+1} = \theta_k - \eta \nabla_\theta \ell(\pi_\theta(\cdot|s), z)\)。
  不同算法对应不同的 \(q_k\)：SFT 使用固定数据集分布 \(d_{\text{data}}\)；RL 使用当前策略的访问分布 \(d_{\pi_{\theta_k}}\)；OPD 使用学生策略的访问分布 \(d_{\pi_S}\)。
- **关键技术细节**：
  - SFT 是**离策略状态拟合**：在固定前缀上施加密集监督，可能导致暴露偏差和灾难性遗忘。
  - RL 是**在策略局部改进**：在模型自身访问的状态上使用（通常稀疏的）奖励信号，更新自然局部化。
  - OPD（在线蒸馏）是**教师引导的在策略学习**：学生采样状态，教师提供局部指导（如连续文本或 soft targets），这类似 DAgger 风格。
- **关键公式**（文字说明）：
  - SFT: 最小化数据集状态上的交叉熵。
  - RL: 最大化当前策略状态和动作序列上的期望奖励。
  - OPD: 给定学生状态，最小化教师分布与学生分布之间的散度（如 KL 散度或交叉熵）。

## 3. 实验设计：数据集、基准、对比方法
- **模型与任务**：Qwen3-0.6B-Base（基础模型），使用 LoRA 适配器。
- **目标任务**：GSM8K（数学推理）。
- **保留任务**（评估遗忘）：TruthfulQA（多项选择）、MMLU（选子集）。
- **基准对比方法**：
  - 基础模型（未训练）
  - 温和 SFT（1 epoch，较低学习率）
  - 压力 SFT（5 epoch，高学习率，高 LoRA rank）
  - OPD 从温和教师（教师为温和 SFT 模型）
  - OPD 从压力教师（教师为压力 SFT 模型）
  - 在线 RL（组相对奖励的 GRPO 风格）
  - 额外消融：一步 OPD（仅匹配下一步词元）—— 效果差（GSM8K 仅 0.040）。
- **评估指标**：GSM8K 准确率、TruthfulQA 准确率、MMLU 准确率；遗忘量（\(F = \text{base} - \text{post}\)）和保留比率；状态漂移用最大均值差异（MMD）衡量。

## 4. 资源与算力
- **硬件**：单张 NVIDIA RTX 3090 24GB GPU。
- **模型**：Qwen3-0.6B-Base（约 6 亿参数）。
- **训练时长**：未明确提及具体小时数；由于是轻量级实验（单卡、小模型、LoRA），训练时间应较短。
- **注意**：作者指出这是“受控小规模研究”，并非大规模基准。

## 5. 实验数量与充分性
- **主要实验数量**：一组主表（表2）包含 7 个配置（基础 + 6 个后训练方法），每个配置在三个任务上报告结果。此外有一步 OPD 的消融实验。
- **充分性评估**：
  - **优点**：实验设计清晰，对比了不同状态源（离策略 vs. 在策略）和不同信号源（密集标注 vs. 奖励 vs. 教师），能直接验证作者的核心预测（P1-P4）。
  - **局限**：规模小，仅一个模型、一个目标任务、两个保留任务、一种适配器（LoRA）。未在更大模型或更多领域任务上验证。RL 实现是简约版 GRPO，非完整 PPO。状态漂移度量仅用词汇特征，未用隐藏状态嵌入。作者承认这些限制，并认为结果应视为对假设的机制性证据，而非基准声明。

## 6. 论文的主要结论与发现
- **发现1**：温和 SFT 可提升目标任务（GSM8K 0.448→0.512）且几乎不遗忘（保留比率 1.0008）；压力 SFT 则导致目标任务下降（0.420）和严重遗忘（保留比率 0.8258）。说明 SFT 本身不是必然遗忘，离策略压力过大时才有害。
- **发现2**：OPD 从压力教师（GSM8K 0.420）出发，在三个任务上都超越了教师（GSM8K 0.466, TruthfulQA 0.275, MMLU 0.430），尽管仅用该教师作为监督。这说明学生通过“在自己的状态上询问教师”可以避免继承教师的不良轨迹分布。
- **发现3**：轻量在线 RL 提升 GSM8K（0.448→0.472）且遗忘极小（保留比率 0.9902），支持 RL 作为在策略局部改进的稳定性。
- **发现4**：状态漂移标量（MMD）不能单独预测遗忘：压力 SFT 和 OPD 从压力教师有几乎相同的 MMD（0.01093 vs. 0.01092），但遗忘差异巨大。说明遗忘取决于训练状态的质量、局部性和可恢复性，而非简单分布距离。

## 7. 优点：方法或实验设计上的亮点
- **理论创新**：将后训练统一到“状态分布+信号源”框架，揭示了 SFT、RL、OPD 之间被忽视的差异，解释了一些反直觉现象（如学生超越教师）。
- **实验设计**：精心控制了变量：温和 vs. 压力 SFT 制造了不同质量的教师；OPD 与 SFT 共享信号源但改变状态源；RL 提供在策略锚点。这些对比直接支撑论点。
- **量化工具**：使用 MMD 作为状态漂移标量，并指出其局限性，避免过度依赖单一指标。
- **代码开源**：提供了 GitHub 仓库，可复现实验。

## 8. 不足与局限
- **规模有限**：仅一个 0.6B 模型、一个目标任务（GSM8K）、两个保留任务。结论在更大模型（如 7B+）和更广泛任务（如代码、对话）上的适用性未知。
- **RL 实现简约**：非标准 PPO/GRPO 的大规模系统，可能忽略了 KL 惩罚等常用约束的影响。
- **状态漂移度量粗糙**：使用词汇特征（TF-IDF 级别）而非句嵌入或模型隐藏状态，可能丢失语义信息。
- **一步 OPD 失败原因**：作者指出一步词元匹配对推理任务太弱，但未深入分析是否可通过温度调节或 Soft target 改进。
- **遗忘衡量**：仅使用两个任务的平均遗忘，可能掩盖对特定能力的差异。
- **未消融 LoRA 的影响**：LoRA 适配器本身可能限制模型移动范围，影响遗忘程度。全参数微调下结论是否相同未知。

（完）
